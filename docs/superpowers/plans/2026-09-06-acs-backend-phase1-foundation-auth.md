# ACS Backend Phase 1 — Foundation & Auth Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Build a running NestJS API with configuration, Postgres/PostGIS, Redis, structured logging, a uniform error model, and complete three-role authentication (customer OTP, operator invite + password, admin password) with revocable rotating refresh tokens.

**Architecture:** A single NestJS process organized as classic layered modules — controller → service → repository — one module per domain. Cross-cutting concerns (auth guard, error filter, request-id logging) are registered globally so protection is opt-out rather than opt-in. Drizzle ORM owns types and queries; migrations are generated by drizzle-kit and hand-augmented where PostGIS and partial indexes exceed the generator.

**Tech Stack:** Node.js 22 LTS · TypeScript strict · NestJS 11 · pnpm · Postgres 16 + PostGIS 3.4 · Drizzle ORM + drizzle-kit · Redis 7 · zod + nestjs-zod · pino · Vitest · @node-rs/argon2 · Twilio Verify

**Spec:** `docs/superpowers/specs/2026-09-02-acs-backend-foundation-booking-design.md`

**Phase 2** (locations, check-in, slots, discovery, bookings) is a separate plan: `2026-09-06-acs-backend-phase2-booking-loop.md`. Do not start it until this plan is complete and reviewed.

## Global Constraints

Every task's requirements implicitly include this section. Values are copied verbatim from the spec.

- **Node.js 22 LTS.** TypeScript with `strict: true` and `noUncheckedIndexedAccess: true`.
- **Package manager is pnpm.** Never npm or yarn.
- **All timestamps are `timestamptz` stored in UTC.** The business timezone (`BUSINESS_TIMEZONE`, default `Asia/Jerusalem`) lives in config and is the only place "today" is computed. No bare `new Date()` for business-day logic.
- **Primary keys are UUIDv7, generated in the application** — never `gen_random_uuid()`, never UUIDv4.
- **Money is `numeric(10,2)` with an explicit ISO-4217 `currency` column.** Never floating point.
- **`jsonb_exists(col, 'en')`, never `col ? 'en'`** — node-postgres parses `?` as a parameter placeholder and the operator form fails at runtime.
- **Passwords: argon2id, m=19 MiB (19456 KiB), t=2, p=1.**
- **Access token: HS256 JWT, 15-minute expiry**, claims `sub`, `role`, `operator_id` (when relevant), `jti`.
- **Refresh token: not a JWT.** 256 bits of opaque random, stored only as a SHA-256 hex hash, 30-day expiry, rotated on every use, grouped by `family_id`.
- **`JwtAuthGuard` is registered globally with `@Public()` as the opt-out.** Protection must fail closed.
- **401 and 403 stay distinct.** 401 = not authenticated; 403 = authenticated but wrong role or not the owner.
- **Every error response uses the envelope** `{ "error": { code, message, details, requestId } }`. `code` is the client contract; `message` is English developer-facing text; `details` carries structured parameters, never prose.
- **No secrets in git.** `.env.example` is committed; `.env` is not.

---

## File Structure

| Path | Responsibility |
|---|---|
| `docker-compose.yml` | postgis + redis for local dev and integration tests |
| `drizzle.config.ts` | drizzle-kit generator configuration |
| `drizzle/` | Generated + hand-augmented SQL migrations (committed) |
| `vitest.unit.config.ts` / `vitest.integration.config.ts` | Two test tiers, separately runnable |
| `src/main.ts` | API entrypoint: bootstrap, helmet, trust proxy, global pipes/filters |
| `src/app.module.ts` | Root module wiring |
| `src/infra/config/env.schema.ts` | zod env schema — the single source of tunables |
| `src/infra/config/config.module.ts` | Validated config provider |
| `src/infra/db/schema/*.ts` | Drizzle table definitions, one file per domain group |
| `src/infra/db/drizzle.module.ts` | Pool + drizzle client provider (`DRIZZLE` token) |
| `src/infra/db/types.ts` | Custom column types (`geographyPoint`, `localizedText`) |
| `src/infra/redis/redis.module.ts` | ioredis client provider (`REDIS` token) |
| `src/common/errors/domain-error.ts` | Typed domain error base + subclasses |
| `src/common/errors/exception.filter.ts` | The one filter that maps errors → HTTP |
| `src/common/request-context/` | Request-id middleware + pino wiring |
| `src/common/auth/` | `JwtAuthGuard`, `RolesGuard`, `@Public`, `@Roles`, `@CurrentUser` |
| `src/common/crypto/` | argon2 password hashing, token hashing, uuidv7 |
| `src/modules/auth/` | Login, OTP, setup, refresh rotation, logout |
| `src/modules/users/` | Identity repository |
| `src/modules/operators/` | Operator profile + admin invite/approve/suspend |
| `test/integration/setup.ts` | Migrate once, truncate between tests |
| `test/e2e/authz-matrix.spec.ts` | Table-driven wrong-role / wrong-owner matrix |

---

## Task 1: Project Scaffold, Config & Liveness

**Files:**
- Create: `package.json`, `tsconfig.json`, `.gitignore`, `.env.example`, `docker-compose.yml`
- Create: `vitest.unit.config.ts`, `vitest.integration.config.ts`
- Create: `src/infra/config/env.schema.ts`, `src/infra/config/config.module.ts`
- Create: `src/app.module.ts`, `src/main.ts`, `src/modules/health/health.controller.ts`, `src/modules/health/health.module.ts`
- Test: `src/infra/config/env.schema.spec.ts`

**Interfaces:**
- Consumes: nothing (first task)
- Produces: `Env` type and `envSchema` (zod) from `src/infra/config/env.schema.ts`; `AppConfigModule` exporting a `ConfigService<Env, true>`; a bootable Nest app on `PORT`.

- [ ] **Step 1: Initialize the project and install dependencies**

```bash
pnpm init
pnpm add @nestjs/common@^11 @nestjs/core@^11 @nestjs/platform-express@^11 @nestjs/config@^4 \
  reflect-metadata rxjs zod nestjs-zod helmet
pnpm add -D typescript @types/node vitest unplugin-swc @swc/core vite-tsconfig-paths \
  @nestjs/testing supertest @types/supertest eslint prettier
```

- [ ] **Step 2: Write `tsconfig.json`**

```json
{
  "compilerOptions": {
    "module": "commonjs",
    "target": "ES2023",
    "lib": ["ES2023"],
    "moduleResolution": "node",
    "outDir": "./dist",
    "rootDir": "./",
    "baseUrl": "./",
    "paths": { "@/*": ["src/*"] },
    "strict": true,
    "noUncheckedIndexedAccess": true,
    "experimentalDecorators": true,
    "emitDecoratorMetadata": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "declaration": false,
    "sourceMap": true
  },
  "include": ["src/**/*", "test/**/*", "*.config.ts"],
  "exclude": ["node_modules", "dist"]
}
```

- [ ] **Step 3: Write `.gitignore` and `docker-compose.yml`**

`.gitignore`:
```
node_modules/
dist/
.env
*.log
coverage/
```

`docker-compose.yml`:
```yaml
services:
  postgres:
    image: postgis/postgis:16-3.4
    environment:
      POSTGRES_USER: acs
      POSTGRES_PASSWORD: acs
      POSTGRES_DB: acs
    ports: ["5432:5432"]
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U acs"]
      interval: 5s
      retries: 10
  redis:
    image: redis:7
    ports: ["6379:6379"]
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      retries: 10
```

- [ ] **Step 4: Write the failing config-schema test**

Create `src/infra/config/env.schema.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { envSchema } from './env.schema';

const required = {
  DATABASE_URL: 'postgres://acs:acs@localhost:5432/acs',
  REDIS_URL: 'redis://localhost:6379',
  JWT_SECRET: 'x'.repeat(32),
};

describe('envSchema', () => {
  it('applies documented defaults', () => {
    const env = envSchema.parse(required);
    expect(env.BUSINESS_TIMEZONE).toBe('Asia/Jerusalem');
    expect(env.SLOT_DURATION_MIN).toBe(15);
    expect(env.DISCOVERY_RADIUS_M).toBe(300);
    expect(env.CHECKIN_LOCATION_TOLERANCE_M).toBe(150);
    expect(env.BOOKING_LEAD_TIME_MIN).toBe(5);
    expect(env.LATE_CANCELLATION_MIN).toBe(60);
    expect(env.DEFAULT_LOCALE).toBe('he');
    expect(env.SUPPORTED_LOCALES).toEqual(['en', 'he']);
  });

  it('rejects a missing DATABASE_URL', () => {
    const { DATABASE_URL, ...rest } = required;
    expect(() => envSchema.parse(rest)).toThrow();
  });

  it('rejects a JWT_SECRET under 32 characters', () => {
    expect(() => envSchema.parse({ ...required, JWT_SECRET: 'short' })).toThrow();
  });

  it('coerces numeric strings from the environment', () => {
    const env = envSchema.parse({ ...required, DISCOVERY_RADIUS_M: '500' });
    expect(env.DISCOVERY_RADIUS_M).toBe(500);
  });

  it('rejects a DEFAULT_LOCALE outside SUPPORTED_LOCALES', () => {
    expect(() =>
      envSchema.parse({ ...required, SUPPORTED_LOCALES: 'en', DEFAULT_LOCALE: 'he' }),
    ).toThrow();
  });
});
```

- [ ] **Step 5: Write the Vitest configs, then run the test to verify it fails**

`vitest.unit.config.ts`:
```ts
import { defineConfig } from 'vitest/config';
import swc from 'unplugin-swc';
import tsconfigPaths from 'vite-tsconfig-paths';

export default defineConfig({
  plugins: [tsconfigPaths(), swc.vite({ module: { type: 'es6' } })],
  test: {
    globals: false,
    include: ['src/**/*.spec.ts'],
    environment: 'node',
  },
});
```

`vitest.integration.config.ts`:
```ts
import { defineConfig } from 'vitest/config';
import swc from 'unplugin-swc';
import tsconfigPaths from 'vite-tsconfig-paths';

export default defineConfig({
  plugins: [tsconfigPaths(), swc.vite({ module: { type: 'es6' } })],
  test: {
    globals: false,
    include: ['test/**/*.spec.ts'],
    environment: 'node',
    setupFiles: ['test/integration/setup.ts'],
    fileParallelism: false,
    testTimeout: 30_000,
  },
});
```

Add to `package.json` scripts:
```json
{
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "start:dev": "node -r ts-node/register src/main.ts",
    "test:unit": "vitest run --config vitest.unit.config.ts",
    "test:integration": "vitest run --config vitest.integration.config.ts"
  }
}
```

Run: `pnpm test:unit`
Expected: FAIL — `Failed to resolve import "./env.schema"`

- [ ] **Step 6: Implement the env schema**

Create `src/infra/config/env.schema.ts`:

```ts
import { z } from 'zod';

const csv = z.string().transform((s) => s.split(',').map((v) => v.trim()).filter(Boolean));

export const envSchema = z
  .object({
    NODE_ENV: z.enum(['development', 'test', 'production']).default('development'),
    PORT: z.coerce.number().int().positive().default(3000),

    DATABASE_URL: z.string().min(1),
    REDIS_URL: z.string().min(1),

    JWT_SECRET: z.string().min(32),
    ACCESS_TOKEN_TTL: z.string().default('15m'),
    REFRESH_TOKEN_TTL: z.string().default('30d'),
    SETUP_TOKEN_TTL: z.string().default('72h'),

    TWILIO_ACCOUNT_SID: z.string().optional(),
    TWILIO_AUTH_TOKEN: z.string().optional(),
    TWILIO_VERIFY_SERVICE_SID: z.string().optional(),

    BUSINESS_TIMEZONE: z.string().default('Asia/Jerusalem'),
    SUPPORTED_LOCALES: csv.default('en,he'),
    DEFAULT_LOCALE: z.string().default('he'),

    SLOT_DURATION_MIN: z.coerce.number().int().positive().default(15),
    DISCOVERY_RADIUS_M: z.coerce.number().int().positive().default(300),
    CHECKIN_LOCATION_TOLERANCE_M: z.coerce.number().int().positive().default(150),
    BOOKING_LEAD_TIME_MIN: z.coerce.number().int().nonnegative().default(5),
    LATE_CANCELLATION_MIN: z.coerce.number().int().nonnegative().default(60),
  })
  .superRefine((env, ctx) => {
    if (!env.SUPPORTED_LOCALES.includes(env.DEFAULT_LOCALE)) {
      ctx.addIssue({
        code: z.ZodIssueCode.custom,
        path: ['DEFAULT_LOCALE'],
        message: 'DEFAULT_LOCALE must be one of SUPPORTED_LOCALES',
      });
    }
  });

export type Env = z.infer<typeof envSchema>;
```

- [ ] **Step 7: Run the test to verify it passes**

Run: `pnpm test:unit`
Expected: PASS — 5 tests

- [ ] **Step 8: Wire the config module, app module, and liveness endpoint**

`src/infra/config/config.module.ts`:
```ts
import { Global, Module } from '@nestjs/common';
import { ConfigModule } from '@nestjs/config';
import { envSchema } from './env.schema';

@Global()
@Module({
  imports: [
    ConfigModule.forRoot({
      isGlobal: true,
      cache: true,
      validate: (raw) => envSchema.parse(raw),
    }),
  ],
})
export class AppConfigModule {}
```

`src/modules/health/health.controller.ts`:
```ts
import { Controller, Get } from '@nestjs/common';

@Controller('health')
export class HealthController {
  @Get()
  live() {
    return { status: 'ok' };
  }
}
```

`src/modules/health/health.module.ts`:
```ts
import { Module } from '@nestjs/common';
import { HealthController } from './health.controller';

@Module({ controllers: [HealthController] })
export class HealthModule {}
```

`src/app.module.ts`:
```ts
import { Module } from '@nestjs/common';
import { AppConfigModule } from './infra/config/config.module';
import { HealthModule } from './modules/health/health.module';

@Module({ imports: [AppConfigModule, HealthModule] })
export class AppModule {}
```

`src/main.ts`:
```ts
import 'reflect-metadata';
import { NestFactory } from '@nestjs/core';
import { ConfigService } from '@nestjs/config';
import helmet from 'helmet';
import { AppModule } from './app.module';
import type { Env } from './infra/config/env.schema';

async function bootstrap() {
  const app = await NestFactory.create(AppModule, { bufferLogs: true });
  const config = app.get(ConfigService<Env, true>);

  app.use(helmet());
  // Behind a TLS-terminating proxy, without this every request appears to come
  // from the load balancer and per-IP rate limits collapse into one global limit.
  app.getHttpAdapter().getInstance().set('trust proxy', 1);

  await app.listen(config.get('PORT', { infer: true }));
}

void bootstrap();
```

- [ ] **Step 9: Write `.env.example` and verify the app boots**

`.env.example`:
```
NODE_ENV=development
PORT=3000
DATABASE_URL=postgres://acs:acs@localhost:5432/acs
REDIS_URL=redis://localhost:6379
JWT_SECRET=replace-me-with-at-least-32-characters-long
BUSINESS_TIMEZONE=Asia/Jerusalem
SUPPORTED_LOCALES=en,he
DEFAULT_LOCALE=he
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_VERIFY_SERVICE_SID=
```

Run: `pnpm typecheck`
Expected: no errors

- [ ] **Step 10: Commit**

```bash
git add .
git commit -m "feat: project scaffold, validated config, liveness endpoint"
```

---

## Task 2: Database, Redis & Integration Test Harness

**Files:**
- Create: `drizzle.config.ts`, `src/infra/db/drizzle.module.ts`, `src/infra/db/schema/index.ts`, `src/infra/db/migrate.ts`
- Create: `src/infra/redis/redis.module.ts`
- Create: `test/integration/setup.ts`, `test/integration/db.helper.ts`
- Modify: `src/modules/health/health.controller.ts`, `src/app.module.ts`
- Test: `test/integration/infra.spec.ts`

**Interfaces:**
- Consumes: `Env` / `ConfigService` from Task 1.
- Produces: injection tokens `DRIZZLE` (a `NodePgDatabase<typeof schema>`) and `REDIS` (an `ioredis` client); `runMigrations(databaseUrl: string): Promise<void>`; test helpers `getTestDb()` and `truncateAll()`.

- [ ] **Step 1: Install database and Redis dependencies**

```bash
pnpm add drizzle-orm pg ioredis uuidv7
pnpm add -D drizzle-kit @types/pg
```

- [ ] **Step 2: Write the failing infrastructure test**

Create `test/integration/infra.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { sql } from 'drizzle-orm';
import { getTestDb, getTestRedis } from './db.helper';

describe('infrastructure', () => {
  it('has the postgis extension installed', async () => {
    const db = getTestDb();
    const rows = await db.execute<{ extname: string }>(
      sql`SELECT extname FROM pg_extension WHERE extname = 'postgis'`,
    );
    expect(rows.rows).toHaveLength(1);
  });

  it('exposes PostGIS distance functions', async () => {
    const db = getTestDb();
    const rows = await db.execute<{ m: number }>(sql`
      SELECT ST_Distance(
        ST_MakePoint(34.7818, 32.0853)::geography,
        ST_MakePoint(34.7818, 32.0863)::geography
      ) AS m
    `);
    expect(Number(rows.rows[0]?.m)).toBeGreaterThan(100);
  });

  it('reaches redis', async () => {
    const redis = getTestRedis();
    await redis.set('probe', 'pong');
    expect(await redis.get('probe')).toBe('pong');
  });
});
```

- [ ] **Step 3: Run the test to verify it fails**

Run: `docker compose up -d` then `pnpm test:integration`
Expected: FAIL — cannot resolve `./db.helper`

- [ ] **Step 4: Implement the drizzle config, schema barrel, and migration runner**

`src/infra/db/schema/index.ts`:
```ts
// Re-exported by every module's schema file. Kept empty until Task 4.
export {};
```

`drizzle.config.ts`:
```ts
import type { Config } from 'drizzle-kit';

export default {
  schema: './src/infra/db/schema/index.ts',
  out: './drizzle',
  dialect: 'postgresql',
  dbCredentials: { url: process.env.DATABASE_URL ?? '' },
  strict: true,
} satisfies Config;
```

`src/infra/db/migrate.ts`:
```ts
import { Pool } from 'pg';
import { drizzle } from 'drizzle-orm/node-postgres';
import { migrate } from 'drizzle-orm/node-postgres/migrator';

export async function runMigrations(databaseUrl: string): Promise<void> {
  const pool = new Pool({ connectionString: databaseUrl, max: 1 });
  try {
    await migrate(drizzle(pool), { migrationsFolder: './drizzle' });
  } finally {
    await pool.end();
  }
}
```

- [ ] **Step 5: Create the PostGIS extension migration by hand**

drizzle-kit will not emit extensions, so this migration is hand-written. Create `drizzle/0000_enable_postgis.sql`:

```sql
CREATE EXTENSION IF NOT EXISTS postgis;
```

Create `drizzle/meta/_journal.json`:
```json
{
  "version": "7",
  "dialect": "postgresql",
  "entries": [
    { "idx": 0, "version": "7", "when": 0, "tag": "0000_enable_postgis", "breakpoints": true }
  ]
}
```

- [ ] **Step 6: Implement the Drizzle and Redis modules**

`src/infra/db/drizzle.module.ts`:
```ts
import { Global, Module, type OnApplicationShutdown } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { Pool } from 'pg';
import { drizzle, type NodePgDatabase } from 'drizzle-orm/node-postgres';
import * as schema from './schema';
import type { Env } from '../config/env.schema';

export const DRIZZLE = Symbol('DRIZZLE');
export const PG_POOL = Symbol('PG_POOL');
export type Db = NodePgDatabase<typeof schema>;

@Global()
@Module({
  providers: [
    {
      provide: PG_POOL,
      inject: [ConfigService],
      useFactory: (config: ConfigService<Env, true>) =>
        new Pool({ connectionString: config.get('DATABASE_URL', { infer: true }) }),
    },
    {
      provide: DRIZZLE,
      inject: [PG_POOL],
      useFactory: (pool: Pool) => drizzle(pool, { schema }),
    },
  ],
  exports: [DRIZZLE, PG_POOL],
})
export class DrizzleModule implements OnApplicationShutdown {
  constructor(private readonly pool: Pool) {}
  async onApplicationShutdown() {
    await this.pool.end();
  }
}
```

`src/infra/redis/redis.module.ts`:
```ts
import { Global, Module, type OnApplicationShutdown, Inject } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import Redis from 'ioredis';
import type { Env } from '../config/env.schema';

export const REDIS = Symbol('REDIS');

@Global()
@Module({
  providers: [
    {
      provide: REDIS,
      inject: [ConfigService],
      useFactory: (config: ConfigService<Env, true>) =>
        new Redis(config.get('REDIS_URL', { infer: true }), { maxRetriesPerRequest: 2 }),
    },
  ],
  exports: [REDIS],
})
export class RedisModule implements OnApplicationShutdown {
  constructor(@Inject(REDIS) private readonly redis: Redis) {}
  async onApplicationShutdown() {
    await this.redis.quit();
  }
}
```

- [ ] **Step 7: Implement the integration test harness**

`test/integration/db.helper.ts`:
```ts
import { Pool } from 'pg';
import { drizzle, type NodePgDatabase } from 'drizzle-orm/node-postgres';
import { sql } from 'drizzle-orm';
import Redis from 'ioredis';
import * as schema from '../../src/infra/db/schema';

let pool: Pool | undefined;
let db: NodePgDatabase<typeof schema> | undefined;
let redis: Redis | undefined;

export const TEST_DATABASE_URL =
  process.env.TEST_DATABASE_URL ?? 'postgres://acs:acs@localhost:5432/acs_test';
export const TEST_REDIS_URL = process.env.TEST_REDIS_URL ?? 'redis://localhost:6379/1';

export function getTestDb() {
  if (!db) {
    pool = new Pool({ connectionString: TEST_DATABASE_URL });
    db = drizzle(pool, { schema });
  }
  return db;
}

export function getTestRedis() {
  redis ??= new Redis(TEST_REDIS_URL);
  return redis;
}

/**
 * Truncates every application table, leaving drizzle's migration bookkeeping
 * intact. RESTART IDENTITY + CASCADE keeps FK order irrelevant.
 */
export async function truncateAll() {
  const database = getTestDb();
  const result = await database.execute<{ tablename: string }>(sql`
    SELECT tablename FROM pg_tables
    WHERE schemaname = 'public' AND tablename NOT LIKE '__drizzle%'
  `);
  const tables = result.rows.map((r) => `"public"."${r.tablename}"`);
  if (tables.length === 0) return;
  await database.execute(sql.raw(`TRUNCATE ${tables.join(', ')} RESTART IDENTITY CASCADE`));
}

export async function closeTestConnections() {
  await pool?.end();
  await redis?.quit();
  pool = undefined;
  db = undefined;
  redis = undefined;
}
```

`test/integration/setup.ts`:
```ts
import { afterAll, afterEach, beforeAll } from 'vitest';
import { Client } from 'pg';
import { runMigrations } from '../../src/infra/db/migrate';
import { closeTestConnections, truncateAll, TEST_DATABASE_URL } from './db.helper';

beforeAll(async () => {
  // Create the dedicated test database if it is absent, then migrate it once
  // for the whole run. Individual tests truncate rather than re-migrate.
  const admin = new Client({ connectionString: 'postgres://acs:acs@localhost:5432/postgres' });
  await admin.connect();
  const name = new URL(TEST_DATABASE_URL).pathname.slice(1);
  const exists = await admin.query('SELECT 1 FROM pg_database WHERE datname = $1', [name]);
  if (exists.rowCount === 0) await admin.query(`CREATE DATABASE "${name}"`);
  await admin.end();

  await runMigrations(TEST_DATABASE_URL);
});

afterEach(async () => {
  await truncateAll();
});

afterAll(async () => {
  await closeTestConnections();
});
```

- [ ] **Step 8: Run the test to verify it passes**

Run: `pnpm test:integration`
Expected: PASS — 3 tests

- [ ] **Step 9: Add the readiness endpoint**

Replace `src/modules/health/health.controller.ts`:
```ts
import { Controller, Get, Inject, ServiceUnavailableException } from '@nestjs/common';
import { sql } from 'drizzle-orm';
import type Redis from 'ioredis';
import { DRIZZLE, type Db } from '../../infra/db/drizzle.module';
import { REDIS } from '../../infra/redis/redis.module';

@Controller('health')
export class HealthController {
  constructor(
    @Inject(DRIZZLE) private readonly db: Db,
    @Inject(REDIS) private readonly redis: Redis,
  ) {}

  @Get()
  live() {
    return { status: 'ok' };
  }

  @Get('ready')
  async ready() {
    try {
      await this.db.execute(sql`SELECT 1`);
      await this.redis.ping();
    } catch (cause) {
      throw new ServiceUnavailableException({ status: 'unavailable' });
    }
    return { status: 'ready', db: 'up', redis: 'up' };
  }
}
```

Update `src/app.module.ts` to import `DrizzleModule` and `RedisModule` alongside the existing imports.

- [ ] **Step 10: Commit**

```bash
git add .
git commit -m "feat: drizzle, redis, postgis migration, integration test harness"
```

---

## Task 3: Logging, Request Context & the Error Model

**Files:**
- Create: `src/common/errors/domain-error.ts`, `src/common/errors/error-codes.ts`, `src/common/errors/exception.filter.ts`
- Create: `src/common/request-context/request-id.middleware.ts`, `src/common/logging/logger.module.ts`
- Modify: `src/app.module.ts`, `src/main.ts`
- Test: `src/common/errors/exception.filter.spec.ts`, `test/e2e/error-envelope.spec.ts`

**Interfaces:**
- Consumes: `Env` from Task 1.
- Produces: `DomainError` base class with subclasses `NotFoundError`, `ConflictError`, `ForbiddenError`, `UnauthorizedError`, `ValidationError`, each carrying `code: string` and `details: Record<string, unknown>`; `AllExceptionsFilter`; `REQUEST_ID_HEADER = 'x-request-id'`.

- [ ] **Step 1: Install logging dependencies**

```bash
pnpm add nestjs-pino pino pino-http
pnpm add -D pino-pretty
```

- [ ] **Step 2: Write the failing filter unit test**

Create `src/common/errors/exception.filter.spec.ts`:

```ts
import { describe, expect, it, vi } from 'vitest';
import { ArgumentsHost, HttpException } from '@nestjs/common';
import { AllExceptionsFilter } from './exception.filter';
import { ConflictError, NotFoundError } from './domain-error';

function hostFor(requestId = 'req-1') {
  const json = vi.fn();
  const status = vi.fn().mockReturnValue({ json });
  const host = {
    switchToHttp: () => ({
      getResponse: () => ({ status }),
      getRequest: () => ({ id: requestId }),
    }),
  } as unknown as ArgumentsHost;
  return { host, status, json };
}

describe('AllExceptionsFilter', () => {
  it('maps a domain ConflictError to 409 with its code and details', () => {
    const { host, status, json } = hostFor();
    new AllExceptionsFilter().catch(
      new ConflictError('SLOT_UNAVAILABLE', 'That slot is no longer available.', {
        startAt: '2026-09-06T08:00:00Z',
      }),
      host,
    );
    expect(status).toHaveBeenCalledWith(409);
    expect(json).toHaveBeenCalledWith({
      error: {
        code: 'SLOT_UNAVAILABLE',
        message: 'That slot is no longer available.',
        details: { startAt: '2026-09-06T08:00:00Z' },
        requestId: 'req-1',
      },
    });
  });

  it('maps a domain NotFoundError to 404', () => {
    const { host, status } = hostFor();
    new AllExceptionsFilter().catch(new NotFoundError('BOOKING_NOT_FOUND', 'No such booking.'), host);
    expect(status).toHaveBeenCalledWith(404);
  });

  it('preserves the status of a Nest HttpException', () => {
    const { host, status, json } = hostFor();
    new AllExceptionsFilter().catch(new HttpException('nope', 403), host);
    expect(status).toHaveBeenCalledWith(403);
    expect(json.mock.calls[0]?.[0].error.code).toBe('FORBIDDEN');
  });

  it('never leaks internals from an unexpected error', () => {
    const { host, status, json } = hostFor();
    new AllExceptionsFilter().catch(new Error('connection string user=admin password=hunter2'), host);
    expect(status).toHaveBeenCalledWith(500);
    expect(json.mock.calls[0]?.[0].error.code).toBe('INTERNAL_ERROR');
    expect(JSON.stringify(json.mock.calls[0]?.[0])).not.toContain('hunter2');
  });
});
```

- [ ] **Step 3: Run the test to verify it fails**

Run: `pnpm test:unit`
Expected: FAIL — cannot resolve `./exception.filter`

- [ ] **Step 4: Implement the domain error hierarchy**

`src/common/errors/domain-error.ts`:
```ts
/**
 * Domain code throws these. Only the exception filter knows about HTTP,
 * so services never import HTTP concerns.
 *
 * `code` is the client contract. `message` is English developer-facing text
 * for logs and is never surfaced to end users. `details` carries structured
 * parameters — never prose — so clients can interpolate localized sentences.
 */
export abstract class DomainError extends Error {
  abstract readonly status: number;

  constructor(
    readonly code: string,
    message: string,
    readonly details: Record<string, unknown> = {},
  ) {
    super(message);
    this.name = new.target.name;
  }
}

export class ValidationError extends DomainError {
  readonly status = 422;
}
export class UnauthorizedError extends DomainError {
  readonly status = 401;
}
export class ForbiddenError extends DomainError {
  readonly status = 403;
}
export class NotFoundError extends DomainError {
  readonly status = 404;
}
export class ConflictError extends DomainError {
  readonly status = 409;
}
export class TooManyRequestsError extends DomainError {
  readonly status = 429;
}
```

`src/common/errors/error-codes.ts`:
```ts
/**
 * The client contract. React Native apps switch on these values to select a
 * localized message; they never parse `message`.
 */
export const ErrorCodes = {
  INTERNAL_ERROR: 'INTERNAL_ERROR',
  VALIDATION_FAILED: 'VALIDATION_FAILED',
  UNAUTHENTICATED: 'UNAUTHENTICATED',
  FORBIDDEN: 'FORBIDDEN',
  INVALID_CREDENTIALS: 'INVALID_CREDENTIALS',
  ACCOUNT_SUSPENDED: 'ACCOUNT_SUSPENDED',
  OPERATOR_NOT_APPROVED: 'OPERATOR_NOT_APPROVED',
  REFRESH_TOKEN_INVALID: 'REFRESH_TOKEN_INVALID',
  REFRESH_TOKEN_REPLAYED: 'REFRESH_TOKEN_REPLAYED',
  SETUP_TOKEN_INVALID: 'SETUP_TOKEN_INVALID',
  OTP_INVALID: 'OTP_INVALID',
  RATE_LIMITED: 'RATE_LIMITED',
  USER_NOT_FOUND: 'USER_NOT_FOUND',
  OPERATOR_NOT_FOUND: 'OPERATOR_NOT_FOUND',
} as const;

export type ErrorCode = (typeof ErrorCodes)[keyof typeof ErrorCodes];
```

- [ ] **Step 5: Implement the exception filter**

`src/common/errors/exception.filter.ts`:
```ts
import { Catch, HttpException, Logger, type ArgumentsHost, type ExceptionFilter } from '@nestjs/common';
import { ZodError } from 'zod';
import { DomainError } from './domain-error';
import { ErrorCodes } from './error-codes';

interface Envelope {
  error: {
    code: string;
    message: string;
    details: Record<string, unknown>;
    requestId: string;
  };
}

@Catch()
export class AllExceptionsFilter implements ExceptionFilter {
  private readonly logger = new Logger(AllExceptionsFilter.name);

  catch(exception: unknown, host: ArgumentsHost): void {
    const http = host.switchToHttp();
    const requestId = String((http.getRequest() as { id?: unknown }).id ?? 'unknown');
    const { status, body } = this.render(exception, requestId);

    if (status >= 500) {
      this.logger.error({ requestId, err: exception }, 'unhandled exception');
    }

    http.getResponse<{ status(code: number): { json(b: Envelope): void } }>()
      .status(status)
      .json(body);
  }

  private render(exception: unknown, requestId: string): { status: number; body: Envelope } {
    if (exception instanceof DomainError) {
      return {
        status: exception.status,
        body: {
          error: {
            code: exception.code,
            message: exception.message,
            details: exception.details,
            requestId,
          },
        },
      };
    }

    if (exception instanceof ZodError) {
      return {
        status: 422,
        body: {
          error: {
            code: ErrorCodes.VALIDATION_FAILED,
            message: 'Request validation failed.',
            // Structured parameters, never prose — the client localizes.
            details: {
              issues: exception.issues.map((i) => ({
                path: i.path.join('.'),
                rule: i.code,
              })),
            },
            requestId,
          },
        },
      };
    }

    if (exception instanceof HttpException) {
      const status = exception.getStatus();
      return {
        status,
        body: {
          error: {
            code: status === 401 ? ErrorCodes.UNAUTHENTICATED : status === 403 ? ErrorCodes.FORBIDDEN : 'HTTP_ERROR',
            message: exception.message,
            details: {},
            requestId,
          },
        },
      };
    }

    return {
      status: 500,
      body: {
        error: {
          code: ErrorCodes.INTERNAL_ERROR,
          message: 'An unexpected error occurred.',
          details: {},
          requestId,
        },
      },
    };
  }
}
```

- [ ] **Step 6: Run the test to verify it passes**

Run: `pnpm test:unit`
Expected: PASS — 4 new tests

- [ ] **Step 7: Wire pino with request-id correlation and register the filter globally**

`src/common/logging/logger.module.ts`:
```ts
import { Module } from '@nestjs/common';
import { LoggerModule } from 'nestjs-pino';
import { uuidv7 } from 'uuidv7';

export const REQUEST_ID_HEADER = 'x-request-id';

@Module({
  imports: [
    LoggerModule.forRoot({
      pinoHttp: {
        genReqId: (req, res) => {
          const incoming = req.headers[REQUEST_ID_HEADER];
          const id = typeof incoming === 'string' && incoming.length > 0 ? incoming : uuidv7();
          res.setHeader(REQUEST_ID_HEADER, id);
          return id;
        },
        // A customer's screenshot must map to a log line in one query, but
        // credentials must never reach the log.
        redact: {
          paths: [
            'req.headers.authorization',
            'req.headers.cookie',
            'req.body.password',
            'req.body.code',
            'req.body.phone',
            'req.body.refreshToken',
          ],
          remove: true,
        },
        transport:
          process.env.NODE_ENV === 'development' ? { target: 'pino-pretty' } : undefined,
      },
    }),
  ],
  exports: [LoggerModule],
})
export class AppLoggerModule {}
```

In `src/main.ts`, after creating the app:
```ts
import { Logger as PinoLogger } from 'nestjs-pino';
import { AllExceptionsFilter } from './common/errors/exception.filter';
import { ZodValidationPipe } from 'nestjs-zod';

app.useLogger(app.get(PinoLogger));
app.useGlobalFilters(new AllExceptionsFilter());
app.useGlobalPipes(new ZodValidationPipe());
```

Add `AppLoggerModule` to `src/app.module.ts` imports.

- [ ] **Step 8: Write and run the e2e envelope test**

Create `test/e2e/error-envelope.spec.ts`:

```ts
import { describe, expect, it, beforeAll, afterAll } from 'vitest';
import { Test } from '@nestjs/testing';
import request from 'supertest';
import type { INestApplication } from '@nestjs/common';
import { AppModule } from '../../src/app.module';
import { AllExceptionsFilter } from '../../src/common/errors/exception.filter';

describe('error envelope', () => {
  let app: INestApplication;

  beforeAll(async () => {
    const moduleRef = await Test.createTestingModule({ imports: [AppModule] }).compile();
    app = moduleRef.createNestApplication();
    app.useGlobalFilters(new AllExceptionsFilter());
    await app.init();
  });

  afterAll(async () => {
    await app.close();
  });

  it('returns the envelope shape and echoes the request id', async () => {
    const res = await request(app.getHttpServer())
      .get('/does-not-exist')
      .set('x-request-id', 'test-req-42');

    expect(res.status).toBe(404);
    expect(res.body.error).toMatchObject({ requestId: 'test-req-42' });
    expect(res.body.error).toHaveProperty('code');
    expect(res.body.error).toHaveProperty('details');
  });
});
```

Run: `pnpm test:integration`
Expected: PASS

- [ ] **Step 9: Commit**

```bash
git add .
git commit -m "feat: domain error model, exception filter, pino request-id logging"
```

---

## Task 4: Identity Schema

**Files:**
- Create: `src/infra/db/schema/enums.ts`, `src/infra/db/schema/users.ts`, `src/infra/db/schema/operators.ts`, `src/infra/db/schema/tokens.ts`
- Modify: `src/infra/db/schema/index.ts`
- Create: `drizzle/0001_identity.sql`
- Test: `test/integration/schema-identity.spec.ts`

**Interfaces:**
- Consumes: `getTestDb`, `truncateAll` from Task 2.
- Produces: drizzle tables `users`, `operators`, `refreshTokens`, `setupTokens`; enums `userRole`, `userStatus`, `operatorApproval`, `operatorPresence`; inferred types `User`, `NewUser`, `Operator`, `NewOperator`.

- [ ] **Step 1: Write the failing schema test**

Create `test/integration/schema-identity.spec.ts`:

```ts
import { describe, expect, it } from 'vitest';
import { uuidv7 } from 'uuidv7';
import { getTestDb } from './db.helper';
import { users, operators } from '../../src/infra/db/schema';

const db = () => getTestDb();

describe('identity schema', () => {
  it('accepts a customer identified only by phone', async () => {
    const [row] = await db()
      .insert(users)
      .values({ id: uuidv7(), role: 'customer', phone: '+972501234567' })
      .returning();
    expect(row?.status).toBe('active');
    expect(row?.preferredLocale).toBe('he');
  });

  it('rejects a customer carrying an email', async () => {
    await expect(
      db().insert(users).values({
        id: uuidv7(),
        role: 'customer',
        phone: '+972501234568',
        email: 'nope@example.com',
      }),
    ).rejects.toThrow(/customer_identity/);
  });

  it('rejects an operator carrying a phone — it would enable OTP login bypass', async () => {
    await expect(
      db().insert(users).values({
        id: uuidv7(),
        role: 'operator',
        email: 'op@example.com',
        phone: '+972501234569',
        displayName: 'Op',
      }),
    ).rejects.toThrow(/staff_identity/);
  });

  it('enforces phone uniqueness', async () => {
    await db().insert(users).values({ id: uuidv7(), role: 'customer', phone: '+972500000001' });
    await expect(
      db().insert(users).values({ id: uuidv7(), role: 'customer', phone: '+972500000001' }),
    ).rejects.toThrow();
  });

  it('links an operator profile 1:1 with its user', async () => {
    const userId = uuidv7();
    await db()
      .insert(users)
      .values({
        id: userId,
        role: 'operator',
        email: 'pilot@example.com',
        displayName: 'Pilot',
        status: 'operator_pending_setup',
      });
    const [op] = await db()
      .insert(operators)
      .values({ id: uuidv7(), userId, displayName: 'Pilot' })
      .returning();

    expect(op?.approvalStatus).toBe('pending');
    expect(op?.presence).toBe('offline');
    expect(op?.gearTags).toEqual([]);

    await expect(
      db().insert(operators).values({ id: uuidv7(), userId, displayName: 'Dupe' }),
    ).rejects.toThrow();
  });
});
```

- [ ] **Step 2: Run the test to verify it fails**

Run: `pnpm test:integration`
Expected: FAIL — `users` is not exported from the schema barrel

- [ ] **Step 3: Implement the enums and users table**

`src/infra/db/schema/enums.ts`:
```ts
import { pgEnum } from 'drizzle-orm/pg-core';

export const userRole = pgEnum('user_role', ['customer', 'operator', 'admin']);
export const userStatus = pgEnum('user_status', [
  'operator_pending_setup',
  'active',
  'suspended',
]);
export const operatorApproval = pgEnum('operator_approval', [
  'pending',
  'approved',
  'rejected',
  'suspended',
]);
export const operatorPresence = pgEnum('operator_presence', [
  'offline',
  'online',
  'in_session',
]);
```

`src/infra/db/schema/users.ts`:
```ts
import { sql } from 'drizzle-orm';
import { check, pgTable, text, timestamp, uuid } from 'drizzle-orm/pg-core';
import { userRole, userStatus } from './enums';

export const users = pgTable(
  'users',
  {
    id: uuid('id').primaryKey(),
    role: userRole('role').notNull(),
    phone: text('phone').unique(),
    email: text('email').unique(),
    passwordHash: text('password_hash'),
    displayName: text('display_name'),
    status: userStatus('status').notNull().default('active'),
    preferredLocale: text('preferred_locale').notNull().default('he'),
    phoneVerifiedAt: timestamp('phone_verified_at', { withTimezone: true }),
    createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
    updatedAt: timestamp('updated_at', { withTimezone: true }).notNull().defaultNow(),
  },
  (t) => [
    check(
      'customer_identity',
      sql`${t.role} <> 'customer' OR (${t.phone} IS NOT NULL AND ${t.email} IS NULL AND ${t.passwordHash} IS NULL)`,
    ),
    // Staff must not carry a phone: users.phone is the customer OTP login
    // identifier, so an operator with one could bypass password auth entirely.
    check(
      'staff_identity',
      sql`${t.role} = 'customer' OR (${t.email} IS NOT NULL AND ${t.phone} IS NULL)`,
    ),
  ],
);

export type User = typeof users.$inferSelect;
export type NewUser = typeof users.$inferInsert;
```

- [ ] **Step 4: Implement the operators and token tables**

`src/infra/db/schema/operators.ts`:
```ts
import { pgTable, text, timestamp, uuid } from 'drizzle-orm/pg-core';
import { operatorApproval, operatorPresence } from './enums';
import { users } from './users';

export const operators = pgTable('operators', {
  id: uuid('id').primaryKey(),
  userId: uuid('user_id').notNull().unique().references(() => users.id),
  displayName: text('display_name').notNull(),
  bio: text('bio'),
  gearTags: text('gear_tags').array().notNull().default(sql`'{}'`),
  approvalStatus: operatorApproval('approval_status').notNull().default('pending'),
  approvedAt: timestamp('approved_at', { withTimezone: true }),
  approvedBy: uuid('approved_by').references(() => users.id),
  presence: operatorPresence('presence').notNull().default('offline'),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
  updatedAt: timestamp('updated_at', { withTimezone: true }).notNull().defaultNow(),
});

export type Operator = typeof operators.$inferSelect;
export type NewOperator = typeof operators.$inferInsert;
```

Add the missing import at the top of that file: `import { sql } from 'drizzle-orm';`

`src/infra/db/schema/tokens.ts`:
```ts
import { index, pgTable, text, timestamp, uuid, type AnyPgColumn } from 'drizzle-orm/pg-core';
import { users } from './users';

export const refreshTokens = pgTable(
  'refresh_tokens',
  {
    id: uuid('id').primaryKey(),
    userId: uuid('user_id').notNull().references(() => users.id),
    familyId: uuid('family_id').notNull(),
    tokenHash: text('token_hash').notNull().unique(),
    deviceInfo: text('device_info'),
    createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
    expiresAt: timestamp('expires_at', { withTimezone: true }).notNull(),
    revokedAt: timestamp('revoked_at', { withTimezone: true }),
    replacedBy: uuid('replaced_by').references((): AnyPgColumn => refreshTokens.id),
  },
  (t) => [
    index('refresh_tokens_user_idx').on(t.userId),
    index('refresh_tokens_family_idx').on(t.familyId),
  ],
);

export const setupTokens = pgTable('setup_tokens', {
  id: uuid('id').primaryKey(),
  userId: uuid('user_id').notNull().references(() => users.id),
  tokenHash: text('token_hash').notNull().unique(),
  createdBy: uuid('created_by').notNull().references(() => users.id),
  createdAt: timestamp('created_at', { withTimezone: true }).notNull().defaultNow(),
  expiresAt: timestamp('expires_at', { withTimezone: true }).notNull(),
  usedAt: timestamp('used_at', { withTimezone: true }),
});

export type RefreshToken = typeof refreshTokens.$inferSelect;
export type SetupToken = typeof setupTokens.$inferSelect;
```

Replace `src/infra/db/schema/index.ts`:
```ts
export * from './enums';
export * from './users';
export * from './operators';
export * from './tokens';
```

- [ ] **Step 5: Generate the migration and verify the CHECK constraints survived**

```bash
pnpm drizzle-kit generate --name identity
```

Open the newly generated `drizzle/0001_identity.sql` and confirm it contains both `CONSTRAINT "customer_identity" CHECK` and `CONSTRAINT "staff_identity" CHECK`. If drizzle-kit omitted them, append by hand:

```sql
ALTER TABLE "users" ADD CONSTRAINT "customer_identity" CHECK (
  role <> 'customer' OR (phone IS NOT NULL AND email IS NULL AND password_hash IS NULL));
ALTER TABLE "users" ADD CONSTRAINT "staff_identity" CHECK (
  role = 'customer' OR (email IS NOT NULL AND phone IS NULL));
```

- [ ] **Step 6: Run the test to verify it passes**

Run: `pnpm test:integration`
Expected: PASS — 5 tests

- [ ] **Step 7: Commit**

```bash
git add .
git commit -m "feat: users, operators, refresh_tokens and setup_tokens schema"
```

---

## Task 5: Crypto Primitives

**Files:**
- Create: `src/common/crypto/password.service.ts`, `src/common/crypto/opaque-token.ts`, `src/common/crypto/crypto.module.ts`
- Test: `src/common/crypto/password.service.spec.ts`, `src/common/crypto/opaque-token.spec.ts`

**Interfaces:**
- Consumes: nothing.
- Produces: `PasswordService` with `hash(plain: string): Promise<string>` and `verify(hash: string, plain: string): Promise<boolean>`; `generateOpaqueToken(): string` (43-char base64url of 256 bits) and `hashToken(token: string): string` (SHA-256 hex); `CryptoModule` exporting `PasswordService`.

- [ ] **Step 1: Install argon2**

```bash
pnpm add @node-rs/argon2
```

`@node-rs/argon2` ships prebuilt binaries, avoiding native build toolchain requirements on Windows.

- [ ] **Step 2: Write the failing crypto tests**

Create `src/common/crypto/password.service.spec.ts`:
```ts
import { describe, expect, it } from 'vitest';
import { PasswordService } from './password.service';

const svc = new PasswordService();

describe('PasswordService', () => {
  it('produces an argon2id hash', async () => {
    const hash = await svc.hash('correct horse battery staple');
    expect(hash.startsWith('$argon2id$')).toBe(true);
  });

  it('verifies a correct password', async () => {
    const hash = await svc.hash('s3cret-passphrase');
    expect(await svc.verify(hash, 's3cret-passphrase')).toBe(true);
  });

  it('rejects an incorrect password', async () => {
    const hash = await svc.hash('s3cret-passphrase');
    expect(await svc.verify(hash, 'wrong')).toBe(false);
  });

  it('salts — the same input hashes differently each time', async () => {
    expect(await svc.hash('same')).not.toBe(await svc.hash('same'));
  });

  it('returns false rather than throwing on a malformed hash', async () => {
    expect(await svc.verify('not-a-hash', 'anything')).toBe(false);
  });
});
```

Create `src/common/crypto/opaque-token.spec.ts`:
```ts
import { describe, expect, it } from 'vitest';
import { generateOpaqueToken, hashToken } from './opaque-token';

describe('opaque tokens', () => {
  it('generates 256 bits as url-safe base64', () => {
    const token = generateOpaqueToken();
    expect(token).toMatch(/^[A-Za-z0-9_-]{43}$/);
  });

  it('generates a distinct value every call', () => {
    const seen = new Set(Array.from({ length: 200 }, () => generateOpaqueToken()));
    expect(seen.size).toBe(200);
  });

  it('hashes deterministically to 64 hex characters', () => {
    expect(hashToken('abc')).toBe(hashToken('abc'));
    expect(hashToken('abc')).toMatch(/^[0-9a-f]{64}$/);
  });

  it('produces different hashes for different tokens', () => {
    expect(hashToken('abc')).not.toBe(hashToken('abd'));
  });
});
```

- [ ] **Step 3: Run the tests to verify they fail**

Run: `pnpm test:unit`
Expected: FAIL — modules not found

- [ ] **Step 4: Implement the primitives**

`src/common/crypto/password.service.ts`:
```ts
import { Injectable } from '@nestjs/common';
import { hash as argonHash, verify as argonVerify, Algorithm } from '@node-rs/argon2';

/** OWASP-recommended argon2id parameters (spec §4.1). */
const OPTIONS = {
  algorithm: Algorithm.Argon2id,
  memoryCost: 19456, // 19 MiB
  timeCost: 2,
  parallelism: 1,
} as const;

@Injectable()
export class PasswordService {
  hash(plain: string): Promise<string> {
    return argonHash(plain, OPTIONS);
  }

  async verify(hash: string, plain: string): Promise<boolean> {
    try {
      return await argonVerify(hash, plain, OPTIONS);
    } catch {
      // A malformed stored hash must read as "does not match", never as a crash
      // that a caller might mistake for a different failure mode.
      return false;
    }
  }
}
```

`src/common/crypto/opaque-token.ts`:
```ts
import { createHash, randomBytes } from 'node:crypto';

/** 256 bits of opaque random. Refresh and setup tokens are never JWTs. */
export function generateOpaqueToken(): string {
  return randomBytes(32).toString('base64url');
}

/** Only the hash is ever persisted, so a database leak yields no usable token. */
export function hashToken(token: string): string {
  return createHash('sha256').update(token).digest('hex');
}
```

`src/common/crypto/crypto.module.ts`:
```ts
import { Global, Module } from '@nestjs/common';
import { PasswordService } from './password.service';

@Global()
@Module({ providers: [PasswordService], exports: [PasswordService] })
export class CryptoModule {}
```

- [ ] **Step 5: Run the tests to verify they pass**

Run: `pnpm test:unit`
Expected: PASS — 9 new tests

- [ ] **Step 6: Commit**

```bash
git add .
git commit -m "feat: argon2id password hashing and opaque token primitives"
```

---

## Task 6: Token Service & Session Repository

**Files:**
- Create: `src/modules/auth/token.service.ts`, `src/modules/auth/session.repository.ts`, `src/modules/auth/auth.types.ts`
- Test: `src/modules/auth/token.service.spec.ts`, `test/integration/session-repository.spec.ts`

**Interfaces:**
- Consumes: `generateOpaqueToken`, `hashToken` (Task 5); `DRIZZLE`/`Db` (Task 2); `users`, `refreshTokens` (Task 4); `Env` (Task 1).
- Produces:
  - `AccessTokenClaims = { sub: string; role: 'customer'|'operator'|'admin'; operatorId?: string; jti: string }`
  - `TokenService.issueAccessToken(claims): string`, `TokenService.verifyAccessToken(token): AccessTokenClaims`
  - `SessionRepository.issue(userId, familyId | null, deviceInfo): Promise<{ token: string; id: string; familyId: string }>`
  - `SessionRepository.findByToken(token): Promise<RefreshToken | undefined>`
  - `SessionRepository.rotate(oldId, userId, familyId, deviceInfo): Promise<{ token: string }>`
  - `SessionRepository.revokeFamily(familyId): Promise<void>`, `revokeById(id)`, `revokeAllForUser(userId)`

- [ ] **Step 1: Install JWT support**

```bash
pnpm add @nestjs/jwt
```

- [ ] **Step 2: Write the failing token service test**

Create `src/modules/auth/token.service.spec.ts`:
```ts
import { describe, expect, it } from 'vitest';
import { JwtService } from '@nestjs/jwt';
import { TokenService } from './token.service';

function build() {
  const jwt = new JwtService({ secret: 'x'.repeat(32), signOptions: { expiresIn: '15m' } });
  return new TokenService(jwt);
}

describe('TokenService', () => {
  it('round-trips the documented claims', () => {
    const svc = build();
    const token = svc.issueAccessToken({
      sub: 'user-1',
      role: 'operator',
      operatorId: 'op-1',
      jti: 'jti-1',
    });
    const claims = svc.verifyAccessToken(token);
    expect(claims).toMatchObject({
      sub: 'user-1',
      role: 'operator',
      operatorId: 'op-1',
      jti: 'jti-1',
    });
  });

  it('omits operatorId for non-operator roles', () => {
    const svc = build();
    const claims = svc.verifyAccessToken(
      svc.issueAccessToken({ sub: 'u', role: 'customer', jti: 'j' }),
    );
    expect(claims.operatorId).toBeUndefined();
  });

  it('rejects a token signed with a different secret', () => {
    const foreign = new TokenService(
      new JwtService({ secret: 'y'.repeat(32), signOptions: { expiresIn: '15m' } }),
    );
    const token = foreign.issueAccessToken({ sub: 'u', role: 'admin', jti: 'j' });
    expect(() => build().verifyAccessToken(token)).toThrow();
  });

  it('rejects an expired token', () => {
    const jwt = new JwtService({ secret: 'x'.repeat(32), signOptions: { expiresIn: '-1s' } });
    const svc = new TokenService(jwt);
    const token = svc.issueAccessToken({ sub: 'u', role: 'admin', jti: 'j' });
    expect(() => svc.verifyAccessToken(token)).toThrow();
  });
});
```

- [ ] **Step 3: Run to verify it fails**

Run: `pnpm test:unit`
Expected: FAIL — cannot resolve `./token.service`

- [ ] **Step 4: Implement the types and token service**

`src/modules/auth/auth.types.ts`:
```ts
export type Role = 'customer' | 'operator' | 'admin';

export interface AccessTokenClaims {
  sub: string;
  role: Role;
  operatorId?: string;
  jti: string;
}

/** Attached to the request by JwtAuthGuard and read by @CurrentUser. */
export interface AuthenticatedUser {
  userId: string;
  role: Role;
  operatorId?: string;
}
```

`src/modules/auth/token.service.ts`:
```ts
import { Injectable } from '@nestjs/common';
import { JwtService } from '@nestjs/jwt';
import type { AccessTokenClaims } from './auth.types';

@Injectable()
export class TokenService {
  constructor(private readonly jwt: JwtService) {}

  issueAccessToken(claims: AccessTokenClaims): string {
    return this.jwt.sign(claims);
  }

  /** Throws on a bad signature or an expired token; callers map that to 401. */
  verifyAccessToken(token: string): AccessTokenClaims {
    return this.jwt.verify<AccessTokenClaims>(token);
  }
}
```

- [ ] **Step 5: Run to verify it passes**

Run: `pnpm test:unit`
Expected: PASS — 4 new tests

- [ ] **Step 6: Write the failing session repository test**

Create `test/integration/session-repository.spec.ts`:
```ts
import { describe, expect, it, beforeEach } from 'vitest';
import { uuidv7 } from 'uuidv7';
import { getTestDb } from './db.helper';
import { users } from '../../src/infra/db/schema';
import { SessionRepository } from '../../src/modules/auth/session.repository';

const REFRESH_TTL_MS = 30 * 24 * 60 * 60 * 1000;
let repo: SessionRepository;
let userId: string;

beforeEach(async () => {
  repo = new SessionRepository(getTestDb(), REFRESH_TTL_MS);
  userId = uuidv7();
  await getTestDb().insert(users).values({ id: userId, role: 'customer', phone: `+9725${Date.now() % 100000000}` });
});

describe('SessionRepository', () => {
  it('issues a token whose plaintext is never stored', async () => {
    const { token } = await repo.issue(userId, null, 'iPhone');
    const row = await repo.findByToken(token);
    expect(row?.tokenHash).not.toContain(token);
    expect(row?.userId).toBe(userId);
  });

  it('starts a new family when none is supplied', async () => {
    const a = await repo.issue(userId, null, null);
    const b = await repo.issue(userId, null, null);
    expect(a.familyId).not.toBe(b.familyId);
  });

  it('rotation marks the old row replaced and keeps the family', async () => {
    const first = await repo.issue(userId, null, null);
    const firstRow = await repo.findByToken(first.token);
    const second = await repo.rotate(firstRow!.id, userId, first.familyId, null);

    const oldRow = await repo.findByToken(first.token);
    const newRow = await repo.findByToken(second.token);

    expect(oldRow?.replacedBy).toBe(newRow?.id);
    expect(newRow?.familyId).toBe(first.familyId);
  });

  it('revokeFamily stamps every member of the family', async () => {
    const first = await repo.issue(userId, null, null);
    const firstRow = await repo.findByToken(first.token);
    const second = await repo.rotate(firstRow!.id, userId, first.familyId, null);

    await repo.revokeFamily(first.familyId);

    expect((await repo.findByToken(first.token))?.revokedAt).not.toBeNull();
    expect((await repo.findByToken(second.token))?.revokedAt).not.toBeNull();
  });

  it('returns undefined for an unknown token', async () => {
    expect(await repo.findByToken('never-issued')).toBeUndefined();
  });
});
```

- [ ] **Step 7: Run to verify it fails, then implement the repository**

Run: `pnpm test:integration` → FAIL (module not found)

`src/modules/auth/session.repository.ts`:
```ts
import { Inject, Injectable } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import { and, eq, isNull } from 'drizzle-orm';
import { uuidv7 } from 'uuidv7';
import ms from 'ms';
import { DRIZZLE, type Db } from '../../infra/db/drizzle.module';
import { refreshTokens, type RefreshToken } from '../../infra/db/schema';
import { generateOpaqueToken, hashToken } from '../../common/crypto/opaque-token';
import type { Env } from '../../infra/config/env.schema';

export const REFRESH_TTL_MS = Symbol('REFRESH_TTL_MS');

@Injectable()
export class SessionRepository {
  constructor(
    @Inject(DRIZZLE) private readonly db: Db,
    @Inject(REFRESH_TTL_MS) private readonly ttlMs: number,
  ) {}

  async issue(userId: string, familyId: string | null, deviceInfo: string | null) {
    const token = generateOpaqueToken();
    const id = uuidv7();
    const family = familyId ?? uuidv7();

    await this.db.insert(refreshTokens).values({
      id,
      userId,
      familyId: family,
      tokenHash: hashToken(token),
      deviceInfo,
      expiresAt: new Date(Date.now() + this.ttlMs),
    });

    return { token, id, familyId: family };
  }

  async findByToken(token: string): Promise<RefreshToken | undefined> {
    const [row] = await this.db
      .select()
      .from(refreshTokens)
      .where(eq(refreshTokens.tokenHash, hashToken(token)))
      .limit(1);
    return row;
  }

  /** Issues the successor and points the predecessor at it, inside one transaction. */
  async rotate(oldId: string, userId: string, familyId: string, deviceInfo: string | null) {
    return this.db.transaction(async (tx) => {
      const token = generateOpaqueToken();
      const id = uuidv7();

      await tx.insert(refreshTokens).values({
        id,
        userId,
        familyId,
        tokenHash: hashToken(token),
        deviceInfo,
        expiresAt: new Date(Date.now() + this.ttlMs),
      });
      await tx.update(refreshTokens).set({ replacedBy: id }).where(eq(refreshTokens.id, oldId));

      return { token, id };
    });
  }

  /**
   * A replayed token is a theft signal, so the whole family dies — not just the
   * presented row. Already-revoked rows keep their original timestamp.
   */
  async revokeFamily(familyId: string): Promise<void> {
    await this.db
      .update(refreshTokens)
      .set({ revokedAt: new Date() })
      .where(and(eq(refreshTokens.familyId, familyId), isNull(refreshTokens.revokedAt)));
  }

  async revokeById(id: string): Promise<void> {
    await this.db
      .update(refreshTokens)
      .set({ revokedAt: new Date() })
      .where(and(eq(refreshTokens.id, id), isNull(refreshTokens.revokedAt)));
  }

  async revokeAllForUser(userId: string): Promise<void> {
    await this.db
      .update(refreshTokens)
      .set({ revokedAt: new Date() })
      .where(and(eq(refreshTokens.userId, userId), isNull(refreshTokens.revokedAt)));
  }
}

export function refreshTtlProvider() {
  return {
    provide: REFRESH_TTL_MS,
    inject: [ConfigService],
    useFactory: (config: ConfigService<Env, true>) =>
      ms(config.get('REFRESH_TOKEN_TTL', { infer: true }) as ms.StringValue),
  };
}
```

Install the duration parser: `pnpm add ms && pnpm add -D @types/ms`

The integration test constructs `SessionRepository` directly with a number, so adjust its constructor call to `new SessionRepository(getTestDb() as never, REFRESH_TTL_MS_VALUE)` where the test defines the literal — the `@Inject` decorators are inert outside the Nest container.

- [ ] **Step 8: Run to verify it passes**

Run: `pnpm test:integration`
Expected: PASS — 5 new tests

- [ ] **Step 9: Commit**

```bash
git add .
git commit -m "feat: access token service and revocable refresh session repository"
```

---

## Task 7: Global Auth Guard, Roles & CurrentUser

**Files:**
- Create: `src/common/auth/public.decorator.ts`, `src/common/auth/roles.decorator.ts`, `src/common/auth/current-user.decorator.ts`
- Create: `src/common/auth/jwt-auth.guard.ts`, `src/common/auth/roles.guard.ts`
- Modify: `src/app.module.ts`
- Test: `test/e2e/guards.spec.ts`

**Interfaces:**
- Consumes: `TokenService`, `AccessTokenClaims`, `AuthenticatedUser` (Task 6).
- Produces: `@Public()`, `@Roles(...roles: Role[])`, `@CurrentUser()` param decorator returning `AuthenticatedUser`; `JwtAuthGuard` and `RolesGuard` registered as `APP_GUARD` in that order.

- [ ] **Step 1: Write the failing guard e2e test**

Create `test/e2e/guards.spec.ts`:
```ts
import { describe, expect, it, beforeAll, afterAll } from 'vitest';
import { Test } from '@nestjs/testing';
import { Controller, Get, INestApplication } from '@nestjs/common';
import { APP_GUARD } from '@nestjs/core';
import { JwtService } from '@nestjs/jwt';
import request from 'supertest';
import { JwtAuthGuard } from '../../src/common/auth/jwt-auth.guard';
import { RolesGuard } from '../../src/common/auth/roles.guard';
import { Public } from '../../src/common/auth/public.decorator';
import { Roles } from '../../src/common/auth/roles.decorator';
import { CurrentUser } from '../../src/common/auth/current-user.decorator';
import { TokenService } from '../../src/modules/auth/token.service';
import type { AuthenticatedUser } from '../../src/modules/auth/auth.types';
import { AllExceptionsFilter } from '../../src/common/errors/exception.filter';

@Controller('probe')
class ProbeController {
  @Public()
  @Get('open')
  open() {
    return { ok: true };
  }

  @Get('protected')
  protectedRoute(@CurrentUser() user: AuthenticatedUser) {
    return { userId: user.userId, role: user.role };
  }

  @Roles('admin')
  @Get('admin-only')
  adminOnly() {
    return { ok: true };
  }
}

const SECRET = 'x'.repeat(32);
let app: INestApplication;
let tokens: TokenService;

beforeAll(async () => {
  const moduleRef = await Test.createTestingModule({
    controllers: [ProbeController],
    providers: [
      { provide: JwtService, useValue: new JwtService({ secret: SECRET, signOptions: { expiresIn: '15m' } }) },
      TokenService,
      { provide: APP_GUARD, useClass: JwtAuthGuard },
      { provide: APP_GUARD, useClass: RolesGuard },
    ],
  }).compile();

  app = moduleRef.createNestApplication();
  app.useGlobalFilters(new AllExceptionsFilter());
  await app.init();
  tokens = moduleRef.get(TokenService);
});

afterAll(async () => {
  await app.close();
});

const bearer = (role: 'customer' | 'operator' | 'admin', sub = 'u1') =>
  `Bearer ${tokens.issueAccessToken({ sub, role, jti: 'j1' })}`;

describe('auth guards', () => {
  it('allows an unauthenticated request to a @Public route', async () => {
    await request(app.getHttpServer()).get('/probe/open').expect(200);
  });

  it('protects an undecorated route by default — 401 without a token', async () => {
    const res = await request(app.getHttpServer()).get('/probe/protected').expect(401);
    expect(res.body.error.code).toBe('UNAUTHENTICATED');
  });

  it('rejects a malformed token with 401', async () => {
    await request(app.getHttpServer())
      .get('/probe/protected')
      .set('Authorization', 'Bearer garbage')
      .expect(401);
  });

  it('populates @CurrentUser from the token', async () => {
    const res = await request(app.getHttpServer())
      .get('/probe/protected')
      .set('Authorization', bearer('customer', 'user-42'))
      .expect(200);
    expect(res.body).toEqual({ userId: 'user-42', role: 'customer' });
  });

  it('returns 403 — not 401 — for an authenticated user with the wrong role', async () => {
    const res = await request(app.getHttpServer())
      .get('/probe/admin-only')
      .set('Authorization', bearer('customer'))
      .expect(403);
    expect(res.body.error.code).toBe('FORBIDDEN');
  });

  it('allows the correct role', async () => {
    await request(app.getHttpServer())
      .get('/probe/admin-only')
      .set('Authorization', bearer('admin'))
      .expect(200);
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `pnpm test:integration`
Expected: FAIL — guard modules not found

- [ ] **Step 3: Implement the decorators**

`src/common/auth/public.decorator.ts`:
```ts
import { SetMetadata } from '@nestjs/common';

export const IS_PUBLIC_KEY = 'auth:isPublic';

/**
 * The only way out of global authentication. Protection is opt-out so that a
 * newly added endpoint is protected because someone had to choose to expose it.
 */
export const Public = () => SetMetadata(IS_PUBLIC_KEY, true);
```

`src/common/auth/roles.decorator.ts`:
```ts
import { SetMetadata } from '@nestjs/common';
import type { Role } from '../../modules/auth/auth.types';

export const ROLES_KEY = 'auth:roles';
export const Roles = (...roles: Role[]) => SetMetadata(ROLES_KEY, roles);
```

`src/common/auth/current-user.decorator.ts`:
```ts
import { createParamDecorator, type ExecutionContext } from '@nestjs/common';
import type { AuthenticatedUser } from '../../modules/auth/auth.types';

export const CurrentUser = createParamDecorator(
  (_data: unknown, ctx: ExecutionContext): AuthenticatedUser =>
    ctx.switchToHttp().getRequest<{ user: AuthenticatedUser }>().user,
);
```

- [ ] **Step 4: Implement the guards**

`src/common/auth/jwt-auth.guard.ts`:
```ts
import { Injectable, type CanActivate, type ExecutionContext } from '@nestjs/common';
import { Reflector } from '@nestjs/core';
import { IS_PUBLIC_KEY } from './public.decorator';
import { UnauthorizedError } from '../errors/domain-error';
import { ErrorCodes } from '../errors/error-codes';
import { TokenService } from '../../modules/auth/token.service';
import type { AuthenticatedUser } from '../../modules/auth/auth.types';

@Injectable()
export class JwtAuthGuard implements CanActivate {
  constructor(
    private readonly reflector: Reflector,
    private readonly tokens: TokenService,
  ) {}

  canActivate(context: ExecutionContext): boolean {
    const isPublic = this.reflector.getAllAndOverride<boolean>(IS_PUBLIC_KEY, [
      context.getHandler(),
      context.getClass(),
    ]);
    if (isPublic) return true;

    const request = context.switchToHttp().getRequest<{
      headers: Record<string, string | string[] | undefined>;
      user?: AuthenticatedUser;
    }>();

    const header = request.headers.authorization;
    const raw = typeof header === 'string' && header.startsWith('Bearer ') ? header.slice(7) : null;
    if (!raw) {
      throw new UnauthorizedError(ErrorCodes.UNAUTHENTICATED, 'Missing bearer token.');
    }

    try {
      const claims = this.tokens.verifyAccessToken(raw);
      request.user = { userId: claims.sub, role: claims.role, operatorId: claims.operatorId };
    } catch {
      throw new UnauthorizedError(ErrorCodes.UNAUTHENTICATED, 'Invalid or expired token.');
    }

    return true;
  }
}
```

`src/common/auth/roles.guard.ts`:
```ts
import { Injectable, type CanActivate, type ExecutionContext } from '@nestjs/common';
import { Reflector } from '@nestjs/core';
import { ROLES_KEY } from './roles.decorator';
import { ForbiddenError } from '../errors/domain-error';
import { ErrorCodes } from '../errors/error-codes';
import type { AuthenticatedUser, Role } from '../../modules/auth/auth.types';

@Injectable()
export class RolesGuard implements CanActivate {
  constructor(private readonly reflector: Reflector) {}

  canActivate(context: ExecutionContext): boolean {
    const required = this.reflector.getAllAndOverride<Role[]>(ROLES_KEY, [
      context.getHandler(),
      context.getClass(),
    ]);
    if (!required || required.length === 0) return true;

    const user = context.switchToHttp().getRequest<{ user?: AuthenticatedUser }>().user;
    // Authenticated but wrong role is 403, never 401 — the distinction keeps
    // client error handling sane (spec §4.3).
    if (!user || !required.includes(user.role)) {
      throw new ForbiddenError(ErrorCodes.FORBIDDEN, 'Insufficient role for this endpoint.');
    }
    return true;
  }
}
```

- [ ] **Step 5: Run to verify it passes**

Run: `pnpm test:integration`
Expected: PASS — 6 tests

- [ ] **Step 6: Commit**

```bash
git add .
git commit -m "feat: global JWT auth guard with @Public opt-out, roles guard, @CurrentUser"
```

---

## Task 8: Password Login

**Files:**
- Create: `src/modules/users/users.repository.ts`, `src/modules/users/users.module.ts`
- Create: `src/modules/auth/auth.service.ts`, `src/modules/auth/auth.controller.ts`, `src/modules/auth/auth.module.ts`, `src/modules/auth/dto/login.dto.ts`
- Modify: `src/app.module.ts`
- Test: `test/e2e/auth-login.spec.ts`

**Interfaces:**
- Consumes: `PasswordService` (5), `TokenService`/`SessionRepository` (6), `users`/`operators` (4).
- Produces:
  - `UsersRepository.findByEmail(email): Promise<User|undefined>`, `findById(id)`, `findByPhone(phone)`, `findOperatorByUserId(userId): Promise<Operator|undefined>`
  - `AuthService.login(email, password, deviceInfo): Promise<TokenPair>` where `TokenPair = { accessToken: string; refreshToken: string; expiresIn: number }`
  - `POST /auth/login`

- [ ] **Step 1: Write the failing login e2e test**

Create `test/e2e/auth-login.spec.ts`:
```ts
import { describe, expect, it, beforeAll, afterAll, beforeEach } from 'vitest';
import request from 'supertest';
import { uuidv7 } from 'uuidv7';
import { getTestDb } from '../integration/db.helper';
import { users, operators } from '../../src/infra/db/schema';
import { PasswordService } from '../../src/common/crypto/password.service';
import { createTestApp, type TestApp } from './app.helper';

let app: TestApp;

beforeAll(async () => {
  app = await createTestApp();
});
afterAll(async () => {
  await app.close();
});

async function seedOperator(opts: {
  email: string;
  password: string;
  userStatus?: 'active' | 'suspended' | 'operator_pending_setup';
  approval?: 'pending' | 'approved' | 'suspended';
}) {
  const userId = uuidv7();
  await getTestDb().insert(users).values({
    id: userId,
    role: 'operator',
    email: opts.email,
    displayName: 'Pilot',
    passwordHash: await new PasswordService().hash(opts.password),
    status: opts.userStatus ?? 'active',
  });
  await getTestDb().insert(operators).values({
    id: uuidv7(),
    userId,
    displayName: 'Pilot',
    approvalStatus: opts.approval ?? 'approved',
  });
  return userId;
}

describe('POST /auth/login', () => {
  it('issues an access and refresh token for valid credentials', async () => {
    await seedOperator({ email: 'pilot@example.com', password: 'correct-horse-battery' });

    const res = await request(app.server)
      .post('/auth/login')
      .send({ email: 'pilot@example.com', password: 'correct-horse-battery' })
      .expect(200);

    expect(res.body.accessToken).toEqual(expect.any(String));
    expect(res.body.refreshToken).toMatch(/^[A-Za-z0-9_-]{43}$/);
  });

  it('is case-insensitive on email', async () => {
    await seedOperator({ email: 'case@example.com', password: 'correct-horse-battery' });
    await request(app.server)
      .post('/auth/login')
      .send({ email: '  CASE@Example.COM ', password: 'correct-horse-battery' })
      .expect(200);
  });

  it('rejects a wrong password with INVALID_CREDENTIALS', async () => {
    await seedOperator({ email: 'wrong@example.com', password: 'correct-horse-battery' });
    const res = await request(app.server)
      .post('/auth/login')
      .send({ email: 'wrong@example.com', password: 'nope' })
      .expect(401);
    expect(res.body.error.code).toBe('INVALID_CREDENTIALS');
  });

  it('gives the same code for an unknown email — no account enumeration', async () => {
    const res = await request(app.server)
      .post('/auth/login')
      .send({ email: 'ghost@example.com', password: 'whatever' })
      .expect(401);
    expect(res.body.error.code).toBe('INVALID_CREDENTIALS');
  });

  it('refuses a suspended account', async () => {
    await seedOperator({
      email: 'susp@example.com',
      password: 'correct-horse-battery',
      userStatus: 'suspended',
    });
    const res = await request(app.server)
      .post('/auth/login')
      .send({ email: 'susp@example.com', password: 'correct-horse-battery' })
      .expect(403);
    expect(res.body.error.code).toBe('ACCOUNT_SUSPENDED');
  });

  it('allows an approved-pending operator to log in — the two gates are independent', async () => {
    await seedOperator({
      email: 'pending@example.com',
      password: 'correct-horse-battery',
      approval: 'pending',
    });
    await request(app.server)
      .post('/auth/login')
      .send({ email: 'pending@example.com', password: 'correct-horse-battery' })
      .expect(200);
  });

  it('embeds operatorId in the access token for operators', async () => {
    await seedOperator({ email: 'claims@example.com', password: 'correct-horse-battery' });
    const res = await request(app.server)
      .post('/auth/login')
      .send({ email: 'claims@example.com', password: 'correct-horse-battery' })
      .expect(200);

    const payload = JSON.parse(
      Buffer.from(String(res.body.accessToken).split('.')[1]!, 'base64url').toString(),
    );
    expect(payload.role).toBe('operator');
    expect(payload.operatorId).toEqual(expect.any(String));
  });
});
```

- [ ] **Step 2: Create the shared e2e app helper**

Create `test/e2e/app.helper.ts`:
```ts
import { Test } from '@nestjs/testing';
import type { INestApplication } from '@nestjs/common';
import { ZodValidationPipe } from 'nestjs-zod';
import { AppModule } from '../../src/app.module';
import { AllExceptionsFilter } from '../../src/common/errors/exception.filter';
import { TEST_DATABASE_URL, TEST_REDIS_URL } from '../integration/db.helper';

export interface TestApp {
  app: INestApplication;
  server: unknown;
  close(): Promise<void>;
}

export async function createTestApp(): Promise<TestApp> {
  process.env.DATABASE_URL = TEST_DATABASE_URL;
  process.env.REDIS_URL = TEST_REDIS_URL;
  process.env.JWT_SECRET ??= 'x'.repeat(32);

  const moduleRef = await Test.createTestingModule({ imports: [AppModule] }).compile();
  const app = moduleRef.createNestApplication();
  app.useGlobalFilters(new AllExceptionsFilter());
  app.useGlobalPipes(new ZodValidationPipe());
  await app.init();

  return {
    app,
    server: app.getHttpServer(),
    close: () => app.close(),
  };
}
```

- [ ] **Step 3: Run to verify it fails**

Run: `pnpm test:integration`
Expected: FAIL — `POST /auth/login` returns 404

- [ ] **Step 4: Implement the users repository**

`src/modules/users/users.repository.ts`:
```ts
import { Inject, Injectable } from '@nestjs/common';
import { eq } from 'drizzle-orm';
import { DRIZZLE, type Db } from '../../infra/db/drizzle.module';
import { operators, users, type Operator, type User } from '../../infra/db/schema';

/** Emails are stored lowercased and trimmed; every lookup normalizes first. */
export function normalizeEmail(email: string): string {
  return email.trim().toLowerCase();
}

@Injectable()
export class UsersRepository {
  constructor(@Inject(DRIZZLE) private readonly db: Db) {}

  async findByEmail(email: string): Promise<User | undefined> {
    const [row] = await this.db
      .select()
      .from(users)
      .where(eq(users.email, normalizeEmail(email)))
      .limit(1);
    return row;
  }

  async findById(id: string): Promise<User | undefined> {
    const [row] = await this.db.select().from(users).where(eq(users.id, id)).limit(1);
    return row;
  }

  async findByPhone(phone: string): Promise<User | undefined> {
    const [row] = await this.db.select().from(users).where(eq(users.phone, phone)).limit(1);
    return row;
  }

  async findOperatorByUserId(userId: string): Promise<Operator | undefined> {
    const [row] = await this.db
      .select()
      .from(operators)
      .where(eq(operators.userId, userId))
      .limit(1);
    return row;
  }
}
```

`src/modules/users/users.module.ts`:
```ts
import { Module } from '@nestjs/common';
import { UsersRepository } from './users.repository';

@Module({ providers: [UsersRepository], exports: [UsersRepository] })
export class UsersModule {}
```

- [ ] **Step 5: Implement the auth service and controller**

`src/modules/auth/dto/login.dto.ts`:
```ts
import { createZodDto } from 'nestjs-zod';
import { z } from 'zod';

export const loginSchema = z.object({
  email: z.string().min(3).max(320),
  password: z.string().min(1).max(1024),
});

export class LoginDto extends createZodDto(loginSchema) {}
```

`src/modules/auth/auth.service.ts`:
```ts
import { Injectable } from '@nestjs/common';
import { uuidv7 } from 'uuidv7';
import { PasswordService } from '../../common/crypto/password.service';
import { ErrorCodes } from '../../common/errors/error-codes';
import { ForbiddenError, UnauthorizedError } from '../../common/errors/domain-error';
import { UsersRepository } from '../users/users.repository';
import { SessionRepository } from './session.repository';
import { TokenService } from './token.service';
import type { Role } from './auth.types';
import type { User } from '../../infra/db/schema';

export interface TokenPair {
  accessToken: string;
  refreshToken: string;
}

@Injectable()
export class AuthService {
  constructor(
    private readonly usersRepo: UsersRepository,
    private readonly passwords: PasswordService,
    private readonly tokens: TokenService,
    private readonly sessions: SessionRepository,
  ) {}

  async login(email: string, password: string, deviceInfo: string | null): Promise<TokenPair> {
    const user = await this.usersRepo.findByEmail(email);

    // Verify against a dummy hash when the user is absent so that response
    // timing does not distinguish "no such account" from "wrong password".
    const hash = user?.passwordHash ?? DUMMY_HASH;
    const ok = await this.passwords.verify(hash, password);

    if (!user || !ok) {
      throw new UnauthorizedError(ErrorCodes.INVALID_CREDENTIALS, 'Email or password is incorrect.');
    }
    if (user.status !== 'active') {
      throw new ForbiddenError(ErrorCodes.ACCOUNT_SUSPENDED, 'This account cannot sign in.', {
        status: user.status,
      });
    }

    return this.issuePair(user, null, deviceInfo);
  }

  /**
   * Builds the claim set and a fresh session. `operatorId` is resolved here so
   * downstream guards never have to hit the database for it.
   */
  async issuePair(user: User, familyId: string | null, deviceInfo: string | null): Promise<TokenPair> {
    const role = user.role as Role;
    const operatorId =
      role === 'operator' ? (await this.usersRepo.findOperatorByUserId(user.id))?.id : undefined;

    const session = familyId
      ? await this.sessions.issue(user.id, familyId, deviceInfo)
      : await this.sessions.issue(user.id, null, deviceInfo);

    return {
      accessToken: this.tokens.issueAccessToken({
        sub: user.id,
        role,
        operatorId,
        jti: uuidv7(),
      }),
      refreshToken: session.token,
    };
  }
}

/** A real argon2id hash of a value nobody knows, used only for timing parity. */
const DUMMY_HASH =
  '$argon2id$v=19$m=19456,t=2,p=1$c29tZXNhbHR2YWx1ZQ$3vTDrJ5wZ8pWpJmPOtGvFQlDgDgTqXcqQoQdVvVJqmM';
```

`src/modules/auth/auth.controller.ts`:
```ts
import { Body, Controller, HttpCode, Post, Req } from '@nestjs/common';
import type { Request } from 'express';
import { Public } from '../../common/auth/public.decorator';
import { AuthService } from './auth.service';
import { LoginDto } from './dto/login.dto';

@Controller('auth')
export class AuthController {
  constructor(private readonly auth: AuthService) {}

  @Public()
  @Post('login')
  @HttpCode(200)
  login(@Body() dto: LoginDto, @Req() req: Request) {
    const deviceInfo = req.get('user-agent') ?? null;
    return this.auth.login(dto.email, dto.password, deviceInfo);
  }
}
```

`src/modules/auth/auth.module.ts`:
```ts
import { Module } from '@nestjs/common';
import { JwtModule } from '@nestjs/jwt';
import { ConfigService } from '@nestjs/config';
import { APP_GUARD } from '@nestjs/core';
import { UsersModule } from '../users/users.module';
import { AuthController } from './auth.controller';
import { AuthService } from './auth.service';
import { TokenService } from './token.service';
import { SessionRepository, refreshTtlProvider } from './session.repository';
import { JwtAuthGuard } from '../../common/auth/jwt-auth.guard';
import { RolesGuard } from '../../common/auth/roles.guard';
import type { Env } from '../../infra/config/env.schema';

@Module({
  imports: [
    UsersModule,
    JwtModule.registerAsync({
      inject: [ConfigService],
      useFactory: (config: ConfigService<Env, true>) => ({
        secret: config.get('JWT_SECRET', { infer: true }),
        signOptions: { expiresIn: config.get('ACCESS_TOKEN_TTL', { infer: true }) },
      }),
    }),
  ],
  controllers: [AuthController],
  providers: [
    AuthService,
    TokenService,
    SessionRepository,
    refreshTtlProvider(),
    { provide: APP_GUARD, useClass: JwtAuthGuard },
    { provide: APP_GUARD, useClass: RolesGuard },
  ],
  exports: [AuthService, TokenService, SessionRepository],
})
export class AuthModule {}
```

Add `AuthModule` and `UsersModule` to `src/app.module.ts` imports.

- [ ] **Step 6: Run to verify it passes**

Run: `pnpm test:integration`
Expected: PASS — 7 tests

- [ ] **Step 7: Commit**

```bash
git add .
git commit -m "feat: password login with status gating and timing-safe credential check"
```

---

## Task 9: Refresh Rotation, Replay Detection & Logout

**Files:**
- Modify: `src/modules/auth/auth.service.ts`, `src/modules/auth/auth.controller.ts`
- Create: `src/modules/auth/dto/refresh.dto.ts`
- Test: `test/e2e/auth-refresh.spec.ts`

**Interfaces:**
- Consumes: `SessionRepository`, `UsersRepository`, `AuthService.issuePair` (Task 8).
- Produces: `AuthService.refresh(token, deviceInfo): Promise<TokenPair>`, `AuthService.logout(token)`, `AuthService.logoutAll(userId)`; `POST /auth/refresh`, `POST /auth/logout`, `POST /auth/logout-all`.

- [ ] **Step 1: Write the failing refresh e2e test**

Create `test/e2e/auth-refresh.spec.ts`:
```ts
import { describe, expect, it, beforeAll, afterAll } from 'vitest';
import request from 'supertest';
import { uuidv7 } from 'uuidv7';
import { eq } from 'drizzle-orm';
import { getTestDb } from '../integration/db.helper';
import { users, operators } from '../../src/infra/db/schema';
import { PasswordService } from '../../src/common/crypto/password.service';
import { createTestApp, type TestApp } from './app.helper';

let app: TestApp;
beforeAll(async () => {
  app = await createTestApp();
});
afterAll(async () => {
  await app.close();
});

async function loginFreshOperator(email: string, approval: 'approved' | 'suspended' = 'approved') {
  const userId = uuidv7();
  await getTestDb().insert(users).values({
    id: userId,
    role: 'operator',
    email,
    displayName: 'Pilot',
    passwordHash: await new PasswordService().hash('correct-horse-battery'),
  });
  await getTestDb()
    .insert(operators)
    .values({ id: uuidv7(), userId, displayName: 'Pilot', approvalStatus: approval });

  const res = await request(app.server)
    .post('/auth/login')
    .send({ email, password: 'correct-horse-battery' })
    .expect(200);

  return { userId, ...res.body as { accessToken: string; refreshToken: string } };
}

describe('POST /auth/refresh', () => {
  it('issues a new pair and rotates the refresh token', async () => {
    const session = await loginFreshOperator('rot@example.com');
    const res = await request(app.server)
      .post('/auth/refresh')
      .send({ refreshToken: session.refreshToken })
      .expect(200);

    expect(res.body.refreshToken).not.toBe(session.refreshToken);
    expect(res.body.accessToken).toEqual(expect.any(String));
  });

  it('rejects an unknown refresh token', async () => {
    const res = await request(app.server)
      .post('/auth/refresh')
      .send({ refreshToken: 'a'.repeat(43) })
      .expect(401);
    expect(res.body.error.code).toBe('REFRESH_TOKEN_INVALID');
  });

  it('detects replay and kills the whole family', async () => {
    const session = await loginFreshOperator('replay@example.com');

    const rotated = await request(app.server)
      .post('/auth/refresh')
      .send({ refreshToken: session.refreshToken })
      .expect(200);

    // Present the already-rotated token: this is the theft signal.
    const replay = await request(app.server)
      .post('/auth/refresh')
      .send({ refreshToken: session.refreshToken })
      .expect(401);
    expect(replay.body.error.code).toBe('REFRESH_TOKEN_REPLAYED');

    // The successor must now be dead too, not merely the replayed row.
    await request(app.server)
      .post('/auth/refresh')
      .send({ refreshToken: rotated.body.refreshToken })
      .expect(401);
  });

  it('refuses to refresh once the user is suspended', async () => {
    const session = await loginFreshOperator('suspend-later@example.com');
    await getTestDb().update(users).set({ status: 'suspended' }).where(eq(users.id, session.userId));

    const res = await request(app.server)
      .post('/auth/refresh')
      .send({ refreshToken: session.refreshToken })
      .expect(403);
    expect(res.body.error.code).toBe('ACCOUNT_SUSPENDED');
  });

  it('refuses to refresh once the operator loses approval', async () => {
    const session = await loginFreshOperator('unapprove@example.com');
    await getTestDb()
      .update(operators)
      .set({ approvalStatus: 'suspended' })
      .where(eq(operators.userId, session.userId));

    const res = await request(app.server)
      .post('/auth/refresh')
      .send({ refreshToken: session.refreshToken })
      .expect(403);
    expect(res.body.error.code).toBe('OPERATOR_NOT_APPROVED');
  });

  it('logout revokes only the presented token', async () => {
    const session = await loginFreshOperator('logout@example.com');
    await request(app.server)
      .post('/auth/logout')
      .send({ refreshToken: session.refreshToken })
      .expect(204);

    await request(app.server)
      .post('/auth/refresh')
      .send({ refreshToken: session.refreshToken })
      .expect(401);
  });

  it('logout-all revokes every session for the user', async () => {
    const a = await loginFreshOperator('logoutall@example.com');
    const b = await request(app.server)
      .post('/auth/login')
      .send({ email: 'logoutall@example.com', password: 'correct-horse-battery' })
      .expect(200);

    await request(app.server)
      .post('/auth/logout-all')
      .set('Authorization', `Bearer ${a.accessToken}`)
      .expect(204);

    await request(app.server).post('/auth/refresh').send({ refreshToken: a.refreshToken }).expect(401);
    await request(app.server)
      .post('/auth/refresh')
      .send({ refreshToken: b.body.refreshToken })
      .expect(401);
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `pnpm test:integration`
Expected: FAIL — `/auth/refresh` returns 404

- [ ] **Step 3: Implement the refresh logic**

Append to `src/modules/auth/auth.service.ts`:
```ts
  /**
   * Three steps, in this order — the order is the security property.
   * 1. Replay check: a token already revoked or rotated is a theft signal,
   *    so the entire family dies, not just the presented row.
   * 2. Re-check current status. This is what actually enforces "approved
   *    operators only" on an ongoing basis; a login-time check alone would
   *    let a suspended operator refresh forever.
   * 3. Only then rotate.
   */
  async refresh(refreshToken: string, deviceInfo: string | null): Promise<TokenPair> {
    const row = await this.sessions.findByToken(refreshToken);
    if (!row) {
      throw new UnauthorizedError(ErrorCodes.REFRESH_TOKEN_INVALID, 'Unknown refresh token.');
    }

    if (row.revokedAt !== null || row.replacedBy !== null) {
      await this.sessions.revokeFamily(row.familyId);
      throw new UnauthorizedError(
        ErrorCodes.REFRESH_TOKEN_REPLAYED,
        'Refresh token was already used; the session family has been revoked.',
      );
    }

    if (row.expiresAt.getTime() <= Date.now()) {
      throw new UnauthorizedError(ErrorCodes.REFRESH_TOKEN_INVALID, 'Refresh token expired.');
    }

    const user = await this.usersRepo.findById(row.userId);
    if (!user) {
      throw new UnauthorizedError(ErrorCodes.REFRESH_TOKEN_INVALID, 'Unknown refresh token.');
    }
    if (user.status !== 'active') {
      throw new ForbiddenError(ErrorCodes.ACCOUNT_SUSPENDED, 'This account cannot sign in.', {
        status: user.status,
      });
    }
    if (user.role === 'operator') {
      const operator = await this.usersRepo.findOperatorByUserId(user.id);
      if (operator?.approvalStatus !== 'approved') {
        throw new ForbiddenError(
          ErrorCodes.OPERATOR_NOT_APPROVED,
          'Operator is not approved to work.',
          { approvalStatus: operator?.approvalStatus ?? 'missing' },
        );
      }
    }

    const rotated = await this.sessions.rotate(row.id, user.id, row.familyId, deviceInfo);
    const role = user.role as Role;
    const operatorId =
      role === 'operator' ? (await this.usersRepo.findOperatorByUserId(user.id))?.id : undefined;

    return {
      accessToken: this.tokens.issueAccessToken({ sub: user.id, role, operatorId, jti: uuidv7() }),
      refreshToken: rotated.token,
    };
  }

  async logout(refreshToken: string): Promise<void> {
    const row = await this.sessions.findByToken(refreshToken);
    if (row) await this.sessions.revokeById(row.id);
  }

  async logoutAll(userId: string): Promise<void> {
    await this.sessions.revokeAllForUser(userId);
  }
```

`src/modules/auth/dto/refresh.dto.ts`:
```ts
import { createZodDto } from 'nestjs-zod';
import { z } from 'zod';

export const refreshSchema = z.object({
  refreshToken: z.string().min(20).max(200),
});

export class RefreshDto extends createZodDto(refreshSchema) {}
```

Append to `src/modules/auth/auth.controller.ts`:
```ts
  @Public()
  @Post('refresh')
  @HttpCode(200)
  refresh(@Body() dto: RefreshDto, @Req() req: Request) {
    return this.auth.refresh(dto.refreshToken, req.get('user-agent') ?? null);
  }

  @Public()
  @Post('logout')
  @HttpCode(204)
  async logout(@Body() dto: RefreshDto) {
    await this.auth.logout(dto.refreshToken);
  }

  @Post('logout-all')
  @HttpCode(204)
  async logoutAll(@CurrentUser() user: AuthenticatedUser) {
    await this.auth.logoutAll(user.userId);
  }
```

Add the imports for `RefreshDto`, `CurrentUser`, and `AuthenticatedUser`.

- [ ] **Step 4: Run to verify it passes**

Run: `pnpm test:integration`
Expected: PASS — 7 tests

- [ ] **Step 5: Commit**

```bash
git add .
git commit -m "feat: refresh rotation with family-wide replay revocation, logout, logout-all"
```

---

## Task 10: Phone Normalization & the OTP Port

**Files:**
- Create: `src/modules/auth/phone.ts`, `src/modules/auth/otp/otp-provider.ts`, `src/modules/auth/otp/twilio-otp.provider.ts`, `src/modules/auth/otp/fake-otp.provider.ts`
- Test: `src/modules/auth/phone.spec.ts`, `src/modules/auth/otp/fake-otp.provider.spec.ts`

**Interfaces:**
- Consumes: `Env` (Task 1).
- Produces: `normalizePhone(raw: string, defaultCountry?: string): string` (throws `ValidationError` on invalid); `OTP_PROVIDER` token; interface `OtpProvider { send(phone: string, locale: string): Promise<void>; check(phone: string, code: string): Promise<boolean> }`; `FakeOtpProvider` with `lastCodeFor(phone)` and a fixed accepted code.

- [ ] **Step 1: Install phone parsing and Twilio**

```bash
pnpm add libphonenumber-js twilio
```

- [ ] **Step 2: Write the failing phone test**

Create `src/modules/auth/phone.spec.ts`:
```ts
import { describe, expect, it } from 'vitest';
import { normalizePhone } from './phone';

describe('normalizePhone', () => {
  it('normalizes a local Israeli number to E.164', () => {
    expect(normalizePhone('050-123-4567', 'IL')).toBe('+972501234567');
  });

  it('normalizes the same number written several ways to one value', () => {
    const forms = ['0501234567', '050 123 4567', '+972 50 123 4567', '+972501234567'];
    const normalized = new Set(forms.map((f) => normalizePhone(f, 'IL')));
    // Without this, per-phone OTP rate limiting is trivially bypassed.
    expect(normalized.size).toBe(1);
  });

  it('accepts an international number regardless of default country', () => {
    expect(normalizePhone('+14155552671', 'IL')).toBe('+14155552671');
  });

  it('rejects a number that is not valid', () => {
    expect(() => normalizePhone('12', 'IL')).toThrow();
  });

  it('rejects free text', () => {
    expect(() => normalizePhone('call me maybe', 'IL')).toThrow();
  });
});
```

- [ ] **Step 3: Run to verify it fails, then implement**

Run: `pnpm test:unit` → FAIL

`src/modules/auth/phone.ts`:
```ts
import { parsePhoneNumberFromString, type CountryCode } from 'libphonenumber-js';
import { ValidationError } from '../../common/errors/domain-error';
import { ErrorCodes } from '../../common/errors/error-codes';

/**
 * Every phone reaching storage or a rate-limit key passes through here.
 * `0501234567` and `+972501234567` must collapse to one value, or per-phone
 * limits count them separately and the OTP abuse guard is defeated.
 */
export function normalizePhone(raw: string, defaultCountry: CountryCode = 'IL'): string {
  const parsed = parsePhoneNumberFromString(raw.trim(), defaultCountry);
  if (!parsed?.isValid()) {
    throw new ValidationError(ErrorCodes.VALIDATION_FAILED, 'Phone number is not valid.', {
      field: 'phone',
    });
  }
  return parsed.number;
}
```

- [ ] **Step 4: Write the failing fake-provider test**

Create `src/modules/auth/otp/fake-otp.provider.spec.ts`:
```ts
import { describe, expect, it } from 'vitest';
import { FakeOtpProvider } from './fake-otp.provider';

describe('FakeOtpProvider', () => {
  it('records the phone and locale it was asked to send to', async () => {
    const provider = new FakeOtpProvider();
    await provider.send('+972501234567', 'he');
    expect(provider.sent).toEqual([{ phone: '+972501234567', locale: 'he' }]);
  });

  it('accepts the fixed test code', async () => {
    const provider = new FakeOtpProvider();
    await provider.send('+972501234567', 'en');
    expect(await provider.check('+972501234567', FakeOtpProvider.CODE)).toBe(true);
  });

  it('rejects any other code', async () => {
    const provider = new FakeOtpProvider();
    await provider.send('+972501234567', 'en');
    expect(await provider.check('+972501234567', '000000')).toBe(false);
  });

  it('rejects a code for a phone that was never sent one', async () => {
    const provider = new FakeOtpProvider();
    expect(await provider.check('+972509999999', FakeOtpProvider.CODE)).toBe(false);
  });
});
```

- [ ] **Step 5: Implement the port and both adapters**

`src/modules/auth/otp/otp-provider.ts`:
```ts
export const OTP_PROVIDER = Symbol('OTP_PROVIDER');

/**
 * The only external boundary in scope, kept to two methods. Twilio Verify owns
 * code generation, SMS delivery, and abuse protection; we never store codes.
 */
export interface OtpProvider {
  send(phone: string, locale: string): Promise<void>;
  check(phone: string, code: string): Promise<boolean>;
}
```

`src/modules/auth/otp/fake-otp.provider.ts`:
```ts
import type { OtpProvider } from './otp-provider';

export class FakeOtpProvider implements OtpProvider {
  static readonly CODE = '424242';

  readonly sent: Array<{ phone: string; locale: string }> = [];
  private readonly pending = new Set<string>();

  async send(phone: string, locale: string): Promise<void> {
    this.sent.push({ phone, locale });
    this.pending.add(phone);
  }

  async check(phone: string, code: string): Promise<boolean> {
    if (!this.pending.has(phone)) return false;
    if (code !== FakeOtpProvider.CODE) return false;
    this.pending.delete(phone);
    return true;
  }
}
```

`src/modules/auth/otp/twilio-otp.provider.ts`:
```ts
import { Injectable } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import twilio, { type Twilio } from 'twilio';
import type { OtpProvider } from './otp-provider';
import type { Env } from '../../../infra/config/env.schema';

@Injectable()
export class TwilioOtpProvider implements OtpProvider {
  private readonly client: Twilio;
  private readonly serviceSid: string;

  constructor(config: ConfigService<Env, true>) {
    this.client = twilio(
      config.get('TWILIO_ACCOUNT_SID', { infer: true }),
      config.get('TWILIO_AUTH_TOKEN', { infer: true }),
    );
    this.serviceSid = config.get('TWILIO_VERIFY_SERVICE_SID', { infer: true }) ?? '';
  }

  async send(phone: string, locale: string): Promise<void> {
    await this.client.verify.v2
      .services(this.serviceSid)
      .verifications.create({ to: phone, channel: 'sms', locale });
  }

  async check(phone: string, code: string): Promise<boolean> {
    const result = await this.client.verify.v2
      .services(this.serviceSid)
      .verificationChecks.create({ to: phone, code });
    return result.status === 'approved';
  }
}
```

- [ ] **Step 6: Run both suites to verify they pass**

Run: `pnpm test:unit`
Expected: PASS — 9 new tests

- [ ] **Step 7: Commit**

```bash
git add .
git commit -m "feat: E.164 phone normalization and OtpProvider port with Twilio and fake adapters"
```

---

## Task 11: Customer OTP Login

**Files:**
- Create: `src/modules/auth/dto/otp.dto.ts`
- Modify: `src/modules/auth/auth.service.ts`, `src/modules/auth/auth.controller.ts`, `src/modules/auth/auth.module.ts`
- Test: `test/e2e/auth-otp.spec.ts`

**Interfaces:**
- Consumes: `normalizePhone`, `OTP_PROVIDER` (10); `issuePair` (8).
- Produces: `AuthService.requestOtp(rawPhone, locale)`, `AuthService.verifyOtp(rawPhone, code, deviceInfo): Promise<TokenPair>`; `POST /auth/otp/request`, `POST /auth/otp/verify`.

- [ ] **Step 1: Write the failing OTP e2e test**

Create `test/e2e/auth-otp.spec.ts`:
```ts
import { describe, expect, it, beforeAll, afterAll } from 'vitest';
import request from 'supertest';
import { eq } from 'drizzle-orm';
import { getTestDb } from '../integration/db.helper';
import { users } from '../../src/infra/db/schema';
import { FakeOtpProvider } from '../../src/modules/auth/otp/fake-otp.provider';
import { OTP_PROVIDER } from '../../src/modules/auth/otp/otp-provider';
import { createTestApp, type TestApp } from './app.helper';

let app: TestApp;
let otp: FakeOtpProvider;

beforeAll(async () => {
  otp = new FakeOtpProvider();
  app = await createTestApp({ overrides: [{ token: OTP_PROVIDER, value: otp }] });
});
afterAll(async () => {
  await app.close();
});

describe('customer OTP', () => {
  it('sends a code to the normalized number in the requested locale', async () => {
    await request(app.server)
      .post('/auth/otp/request')
      .send({ phone: '050-123-4567', locale: 'he' })
      .expect(204);

    expect(otp.sent.at(-1)).toEqual({ phone: '+972501234567', locale: 'he' });
  });

  it('creates the customer on first successful verify', async () => {
    await request(app.server).post('/auth/otp/request').send({ phone: '0502222222' }).expect(204);

    const res = await request(app.server)
      .post('/auth/otp/verify')
      .send({ phone: '0502222222', code: FakeOtpProvider.CODE })
      .expect(200);

    expect(res.body.accessToken).toEqual(expect.any(String));

    const [row] = await getTestDb().select().from(users).where(eq(users.phone, '+972502222222'));
    expect(row?.role).toBe('customer');
    expect(row?.phoneVerifiedAt).not.toBeNull();
  });

  it('reuses the existing customer on a second login', async () => {
    await request(app.server).post('/auth/otp/request').send({ phone: '0503333333' }).expect(204);
    await request(app.server)
      .post('/auth/otp/verify')
      .send({ phone: '0503333333', code: FakeOtpProvider.CODE })
      .expect(200);

    await request(app.server).post('/auth/otp/request').send({ phone: '+972503333333' }).expect(204);
    await request(app.server)
      .post('/auth/otp/verify')
      .send({ phone: '+972503333333', code: FakeOtpProvider.CODE })
      .expect(200);

    const rows = await getTestDb().select().from(users).where(eq(users.phone, '+972503333333'));
    expect(rows).toHaveLength(1);
  });

  it('persists preferred_locale from the request', async () => {
    await request(app.server)
      .post('/auth/otp/request')
      .send({ phone: '0504444444', locale: 'en' })
      .expect(204);
    await request(app.server)
      .post('/auth/otp/verify')
      .send({ phone: '0504444444', code: FakeOtpProvider.CODE })
      .expect(200);

    const [row] = await getTestDb().select().from(users).where(eq(users.phone, '+972504444444'));
    expect(row?.preferredLocale).toBe('en');
  });

  it('rejects a wrong code with OTP_INVALID', async () => {
    await request(app.server).post('/auth/otp/request').send({ phone: '0505555555' }).expect(204);
    const res = await request(app.server)
      .post('/auth/otp/verify')
      .send({ phone: '0505555555', code: '999999' })
      .expect(401);
    expect(res.body.error.code).toBe('OTP_INVALID');
  });

  it('rejects an invalid phone number with 422', async () => {
    await request(app.server).post('/auth/otp/request').send({ phone: '12' }).expect(422);
  });

  it('rejects an unsupported locale with 422', async () => {
    await request(app.server)
      .post('/auth/otp/request')
      .send({ phone: '0506666666', locale: 'fr' })
      .expect(422);
  });
});
```

- [ ] **Step 2: Extend the e2e app helper to accept provider overrides**

Modify `test/e2e/app.helper.ts` — change the signature and builder:
```ts
export interface CreateTestAppOptions {
  overrides?: Array<{ token: unknown; value: unknown }>;
}

export async function createTestApp(options: CreateTestAppOptions = {}): Promise<TestApp> {
  process.env.DATABASE_URL = TEST_DATABASE_URL;
  process.env.REDIS_URL = TEST_REDIS_URL;
  process.env.JWT_SECRET ??= 'x'.repeat(32);

  let builder = Test.createTestingModule({ imports: [AppModule] });
  for (const o of options.overrides ?? []) {
    builder = builder.overrideProvider(o.token).useValue(o.value);
  }
  const moduleRef = await builder.compile();
  // ...unchanged from here
}
```

- [ ] **Step 3: Run to verify it fails**

Run: `pnpm test:integration`
Expected: FAIL — `/auth/otp/request` returns 404

- [ ] **Step 4: Implement the DTOs**

`src/modules/auth/dto/otp.dto.ts`:
```ts
import { createZodDto } from 'nestjs-zod';
import { z } from 'zod';

const supported = (process.env.SUPPORTED_LOCALES ?? 'en,he').split(',').map((s) => s.trim());

export const otpRequestSchema = z.object({
  phone: z.string().min(5).max(32),
  locale: z.enum(supported as [string, ...string[]]).optional(),
});

export const otpVerifySchema = z.object({
  phone: z.string().min(5).max(32),
  code: z.string().regex(/^\d{4,10}$/),
});

export class OtpRequestDto extends createZodDto(otpRequestSchema) {}
export class OtpVerifyDto extends createZodDto(otpVerifySchema) {}
```

- [ ] **Step 5: Implement the service methods**

Append to `src/modules/auth/auth.service.ts` (adding `@Inject(OTP_PROVIDER) private readonly otp: OtpProvider` and `ConfigService` to the constructor):
```ts
  async requestOtp(rawPhone: string, locale?: string): Promise<void> {
    const phone = normalizePhone(rawPhone);
    const chosen = locale ?? this.config.get('DEFAULT_LOCALE', { infer: true });
    await this.otp.send(phone, chosen);
    // Remember the locale for the pending verification so the created user
    // gets it even though `verify` carries no locale of its own.
    this.pendingLocale.set(phone, chosen);
  }

  async verifyOtp(rawPhone: string, code: string, deviceInfo: string | null): Promise<TokenPair> {
    const phone = normalizePhone(rawPhone);
    const ok = await this.otp.check(phone, code);
    if (!ok) {
      throw new UnauthorizedError(ErrorCodes.OTP_INVALID, 'The verification code is not valid.');
    }

    const locale = this.pendingLocale.get(phone) ?? this.config.get('DEFAULT_LOCALE', { infer: true });
    this.pendingLocale.delete(phone);

    const user = await this.usersRepo.upsertCustomerByPhone(phone, locale);
    return this.issuePair(user, null, deviceInfo);
  }
```

Add the field `private readonly pendingLocale = new Map<string, string>();` to `AuthService`.

Append to `src/modules/users/users.repository.ts`:
```ts
  /**
   * Customers are created on first successful OTP verification. `display_name`
   * stays null — the OTP flow supplies no name; the client prompts later.
   */
  async upsertCustomerByPhone(phone: string, locale: string): Promise<User> {
    const existing = await this.findByPhone(phone);
    if (existing) {
      const [updated] = await this.db
        .update(users)
        .set({ preferredLocale: locale, phoneVerifiedAt: new Date(), updatedAt: new Date() })
        .where(eq(users.id, existing.id))
        .returning();
      return updated!;
    }

    const [created] = await this.db
      .insert(users)
      .values({
        id: uuidv7(),
        role: 'customer',
        phone,
        preferredLocale: locale,
        phoneVerifiedAt: new Date(),
      })
      .returning();
    return created!;
  }
```

Add `import { uuidv7 } from 'uuidv7';` to that file.

- [ ] **Step 6: Add the controller routes and register the provider**

Append to `src/modules/auth/auth.controller.ts`:
```ts
  @Public()
  @Post('otp/request')
  @HttpCode(204)
  async requestOtp(@Body() dto: OtpRequestDto) {
    await this.auth.requestOtp(dto.phone, dto.locale);
  }

  @Public()
  @Post('otp/verify')
  @HttpCode(200)
  verifyOtp(@Body() dto: OtpVerifyDto, @Req() req: Request) {
    return this.auth.verifyOtp(dto.phone, dto.code, req.get('user-agent') ?? null);
  }
```

In `src/modules/auth/auth.module.ts` providers, add:
```ts
    { provide: OTP_PROVIDER, useClass: TwilioOtpProvider },
```

- [ ] **Step 7: Run to verify it passes**

Run: `pnpm test:integration`
Expected: PASS — 7 tests

- [ ] **Step 8: Commit**

```bash
git add .
git commit -m "feat: customer OTP login with locale persistence and phone normalization"
```

---

## Task 12: Redis Rate Limiting

**Files:**
- Create: `src/common/rate-limit/rate-limiter.service.ts`, `src/common/rate-limit/rate-limit.module.ts`
- Modify: `src/modules/auth/auth.service.ts`, `src/modules/auth/auth.controller.ts`
- Test: `test/integration/rate-limiter.spec.ts`, `test/e2e/auth-rate-limit.spec.ts`

**Interfaces:**
- Consumes: `REDIS` (Task 2); `TooManyRequestsError` (Task 3).
- Produces: `RateLimiterService.consume(key: string, limit: number, windowSeconds: number): Promise<void>` — throws `TooManyRequestsError` with `details.retryAfterSeconds` when exceeded; `RateLimiterService.reset(key)`.

- [ ] **Step 1: Write the failing limiter test**

Create `test/integration/rate-limiter.spec.ts`:
```ts
import { describe, expect, it, beforeEach } from 'vitest';
import { getTestRedis } from './db.helper';
import { RateLimiterService } from '../../src/common/rate-limit/rate-limiter.service';

let limiter: RateLimiterService;

beforeEach(async () => {
  await getTestRedis().flushdb();
  limiter = new RateLimiterService(getTestRedis());
});

describe('RateLimiterService', () => {
  it('permits calls up to the limit', async () => {
    await limiter.consume('k', 3, 60);
    await limiter.consume('k', 3, 60);
    await expect(limiter.consume('k', 3, 60)).resolves.toBeUndefined();
  });

  it('throws once the limit is exceeded', async () => {
    for (let i = 0; i < 3; i++) await limiter.consume('k', 3, 60);
    await expect(limiter.consume('k', 3, 60)).rejects.toMatchObject({ status: 429 });
  });

  it('reports a positive retryAfterSeconds', async () => {
    for (let i = 0; i < 2; i++) await limiter.consume('k', 2, 60);
    await limiter.consume('k', 2, 60).catch((e: { details: { retryAfterSeconds: number } }) => {
      expect(e.details.retryAfterSeconds).toBeGreaterThan(0);
      expect(e.details.retryAfterSeconds).toBeLessThanOrEqual(60);
    });
  });

  it('tracks distinct keys independently', async () => {
    await limiter.consume('a', 1, 60);
    await expect(limiter.consume('b', 1, 60)).resolves.toBeUndefined();
  });

  it('reset clears the counter', async () => {
    await limiter.consume('k', 1, 60);
    await limiter.reset('k');
    await expect(limiter.consume('k', 1, 60)).resolves.toBeUndefined();
  });
});
```

- [ ] **Step 2: Run to verify it fails, then implement**

Run: `pnpm test:integration` → FAIL

`src/common/rate-limit/rate-limiter.service.ts`:
```ts
import { Inject, Injectable } from '@nestjs/common';
import type Redis from 'ioredis';
import { REDIS } from '../../infra/redis/redis.module';
import { TooManyRequestsError } from '../errors/domain-error';
import { ErrorCodes } from '../errors/error-codes';

@Injectable()
export class RateLimiterService {
  constructor(@Inject(REDIS) private readonly redis: Redis) {}

  /**
   * Fixed-window counter. INCR then EXPIRE only on first hit, so the window
   * starts at the first request rather than sliding forward on every call.
   */
  async consume(key: string, limit: number, windowSeconds: number): Promise<void> {
    const redisKey = `rl:${key}`;
    const count = await this.redis.incr(redisKey);
    if (count === 1) {
      await this.redis.expire(redisKey, windowSeconds);
    }
    if (count > limit) {
      const ttl = await this.redis.ttl(redisKey);
      throw new TooManyRequestsError(ErrorCodes.RATE_LIMITED, 'Too many attempts.', {
        retryAfterSeconds: ttl > 0 ? ttl : windowSeconds,
      });
    }
  }

  async reset(key: string): Promise<void> {
    await this.redis.del(`rl:${key}`);
  }
}
```

`src/common/rate-limit/rate-limit.module.ts`:
```ts
import { Global, Module } from '@nestjs/common';
import { RateLimiterService } from './rate-limiter.service';

@Global()
@Module({ providers: [RateLimiterService], exports: [RateLimiterService] })
export class RateLimitModule {}
```

- [ ] **Step 3: Run to verify it passes**

Run: `pnpm test:integration`
Expected: PASS — 5 tests

- [ ] **Step 4: Write the failing endpoint rate-limit test**

Create `test/e2e/auth-rate-limit.spec.ts`:
```ts
import { describe, expect, it, beforeAll, afterAll, beforeEach } from 'vitest';
import request from 'supertest';
import { getTestRedis } from '../integration/db.helper';
import { FakeOtpProvider } from '../../src/modules/auth/otp/fake-otp.provider';
import { OTP_PROVIDER } from '../../src/modules/auth/otp/otp-provider';
import { createTestApp, type TestApp } from './app.helper';

let app: TestApp;

beforeAll(async () => {
  app = await createTestApp({ overrides: [{ token: OTP_PROVIDER, value: new FakeOtpProvider() }] });
});
beforeEach(async () => {
  await getTestRedis().flushdb();
});
afterAll(async () => {
  await app.close();
});

describe('auth rate limiting', () => {
  it('limits repeated OTP requests for one phone', async () => {
    for (let i = 0; i < 3; i++) {
      await request(app.server).post('/auth/otp/request').send({ phone: '0507770001' }).expect(204);
    }
    const res = await request(app.server)
      .post('/auth/otp/request')
      .send({ phone: '0507770001' })
      .expect(429);

    expect(res.body.error.code).toBe('RATE_LIMITED');
    expect(res.body.error.details.retryAfterSeconds).toBeGreaterThan(0);
  });

  it('counts normalized forms of the same number together', async () => {
    await request(app.server).post('/auth/otp/request').send({ phone: '0507770002' }).expect(204);
    await request(app.server).post('/auth/otp/request').send({ phone: '+972507770002' }).expect(204);
    await request(app.server).post('/auth/otp/request').send({ phone: '050-777-0002' }).expect(204);
    await request(app.server).post('/auth/otp/request').send({ phone: '0507770002' }).expect(429);
  });

  it('limits repeated failed logins for one email', async () => {
    for (let i = 0; i < 5; i++) {
      await request(app.server)
        .post('/auth/login')
        .send({ email: 'brute@example.com', password: 'nope' })
        .expect(401);
    }
    await request(app.server)
      .post('/auth/login')
      .send({ email: 'brute@example.com', password: 'nope' })
      .expect(429);
  });
});
```

- [ ] **Step 5: Apply limits in the service**

In `AuthService`, inject `RateLimiterService` and add guards at the top of each method:

```ts
  // OTP request: 3 per phone per hour, 20 per IP per hour.
  async requestOtp(rawPhone: string, locale: string | undefined, ip: string): Promise<void> {
    const phone = normalizePhone(rawPhone);
    await this.limiter.consume(`otp:phone:${phone}`, 3, 3600);
    await this.limiter.consume(`otp:ip:${ip}`, 20, 3600);
    // ...existing body
  }

  // OTP verify: 5 attempts per phone per 15 minutes.
  async verifyOtp(rawPhone: string, code: string, deviceInfo: string | null): Promise<TokenPair> {
    const phone = normalizePhone(rawPhone);
    await this.limiter.consume(`otpcheck:${phone}`, 5, 900);
    // ...existing body
  }

  // Login: 5 per email per 15 minutes, 30 per IP per 15 minutes.
  async login(email: string, password: string, deviceInfo: string | null, ip: string) {
    const key = normalizeEmail(email);
    await this.limiter.consume(`login:email:${key}`, 5, 900);
    await this.limiter.consume(`login:ip:${ip}`, 30, 900);
    // ...existing body
  }
```

On a *successful* login, clear the email counter so a legitimate user is not punished for earlier typos: `await this.limiter.reset(\`login:email:${key}\`);` immediately before `return this.issuePair(...)`.

Pass `req.ip` from the controller into `login` and `requestOtp`. `trust proxy` is already configured in `main.ts`, so `req.ip` is the real client address rather than the load balancer's.

- [ ] **Step 6: Run to verify it passes**

Run: `pnpm test:integration`
Expected: PASS — 3 tests

- [ ] **Step 7: Commit**

```bash
git add .
git commit -m "feat: redis rate limiting on login and OTP endpoints"
```

---

## Task 13: Operator Provisioning & Account Setup

**Files:**
- Create: `src/modules/operators/operators.repository.ts`, `src/modules/operators/admin-operators.controller.ts`, `src/modules/operators/operators.service.ts`, `src/modules/operators/operators.module.ts`, `src/modules/operators/dto/create-operator.dto.ts`
- Create: `src/modules/auth/dto/setup.dto.ts`
- Modify: `src/modules/auth/auth.service.ts`, `src/modules/auth/auth.controller.ts`
- Test: `test/e2e/operator-provisioning.spec.ts`

**Interfaces:**
- Consumes: `PasswordService`, `generateOpaqueToken`/`hashToken`, `UsersRepository`, guards.
- Produces:
  - `OperatorsService.invite(input, adminUserId): Promise<{ operatorId: string; setupToken: string }>`
  - `OperatorsService.approve(operatorId, adminUserId)`, `suspend(operatorId)`, `list(filter)`
  - `AuthService.completeSetup(token, password): Promise<void>`
  - `POST /admin/operators`, `GET /admin/operators`, `POST /admin/operators/:id/approve`, `POST /admin/operators/:id/suspend`, `POST /auth/setup/:token`

- [ ] **Step 1: Write the failing provisioning e2e test**

Create `test/e2e/operator-provisioning.spec.ts`:
```ts
import { describe, expect, it, beforeAll, afterAll } from 'vitest';
import request from 'supertest';
import { uuidv7 } from 'uuidv7';
import { eq } from 'drizzle-orm';
import { getTestDb } from '../integration/db.helper';
import { users, operators } from '../../src/infra/db/schema';
import { createTestApp, type TestApp } from './app.helper';
import { TokenService } from '../../src/modules/auth/token.service';

let app: TestApp;
let adminAuth: string;

beforeAll(async () => {
  app = await createTestApp();
  const adminId = uuidv7();
  await getTestDb()
    .insert(users)
    .values({ id: adminId, role: 'admin', email: 'admin@example.com', displayName: 'Admin' });
  const tokens = app.app.get(TokenService);
  adminAuth = `Bearer ${tokens.issueAccessToken({ sub: adminId, role: 'admin', jti: uuidv7() })}`;
});
afterAll(async () => {
  await app.close();
});

const invite = (email: string) =>
  request(app.server)
    .post('/admin/operators')
    .set('Authorization', adminAuth)
    .send({ email, displayName: 'New Pilot', preferredLocale: 'he' });

describe('operator provisioning', () => {
  it('creates the user, the operator profile, and a setup token', async () => {
    const res = await invite('newpilot@example.com').expect(201);
    expect(res.body.operatorId).toEqual(expect.any(String));
    expect(res.body.setupToken).toMatch(/^[A-Za-z0-9_-]{43}$/);

    const [user] = await getTestDb()
      .select()
      .from(users)
      .where(eq(users.email, 'newpilot@example.com'));
    expect(user?.status).toBe('operator_pending_setup');
    expect(user?.passwordHash).toBeNull();
    expect(user?.preferredLocale).toBe('he');
  });

  it('refuses provisioning by a non-admin', async () => {
    const tokens = app.app.get(TokenService);
    const customer = `Bearer ${tokens.issueAccessToken({ sub: uuidv7(), role: 'customer', jti: uuidv7() })}`;
    await request(app.server)
      .post('/admin/operators')
      .set('Authorization', customer)
      .send({ email: 'x@example.com', displayName: 'X' })
      .expect(403);
  });

  it('refuses provisioning without a token at all', async () => {
    await request(app.server)
      .post('/admin/operators')
      .send({ email: 'y@example.com', displayName: 'Y' })
      .expect(401);
  });

  it('cannot log in before setup completes', async () => {
    await invite('presetup@example.com').expect(201);
    await request(app.server)
      .post('/auth/login')
      .send({ email: 'presetup@example.com', password: 'anything-at-all' })
      .expect(401);
  });

  it('setup sets the password and activates the account', async () => {
    const created = await invite('setup@example.com').expect(201);

    await request(app.server)
      .post(`/auth/setup/${created.body.setupToken}`)
      .send({ password: 'a-good-long-password' })
      .expect(204);

    const [user] = await getTestDb().select().from(users).where(eq(users.email, 'setup@example.com'));
    expect(user?.status).toBe('active');
    expect(user?.passwordHash).not.toBeNull();
  });

  it('rejects a reused setup token', async () => {
    const created = await invite('reuse@example.com').expect(201);
    await request(app.server)
      .post(`/auth/setup/${created.body.setupToken}`)
      .send({ password: 'a-good-long-password' })
      .expect(204);

    const res = await request(app.server)
      .post(`/auth/setup/${created.body.setupToken}`)
      .send({ password: 'another-good-password' })
      .expect(401);
    expect(res.body.error.code).toBe('SETUP_TOKEN_INVALID');
  });

  it('rejects an unknown setup token', async () => {
    await request(app.server)
      .post(`/auth/setup/${'z'.repeat(43)}`)
      .send({ password: 'a-good-long-password' })
      .expect(401);
  });

  it('approve flips approval_status and records the approver', async () => {
    const created = await invite('approve@example.com').expect(201);
    await request(app.server)
      .post(`/admin/operators/${created.body.operatorId}/approve`)
      .set('Authorization', adminAuth)
      .expect(204);

    const [op] = await getTestDb()
      .select()
      .from(operators)
      .where(eq(operators.id, created.body.operatorId));
    expect(op?.approvalStatus).toBe('approved');
    expect(op?.approvedBy).not.toBeNull();
    expect(op?.approvedAt).not.toBeNull();
  });

  it('suspend revokes every refresh token for that operator', async () => {
    const created = await invite('suspend@example.com').expect(201);
    await request(app.server)
      .post(`/auth/setup/${created.body.setupToken}`)
      .send({ password: 'a-good-long-password' })
      .expect(204);
    await request(app.server)
      .post(`/admin/operators/${created.body.operatorId}/approve`)
      .set('Authorization', adminAuth)
      .expect(204);

    const login = await request(app.server)
      .post('/auth/login')
      .send({ email: 'suspend@example.com', password: 'a-good-long-password' })
      .expect(200);

    await request(app.server)
      .post(`/admin/operators/${created.body.operatorId}/suspend`)
      .set('Authorization', adminAuth)
      .expect(204);

    // Refresh must fail immediately — this is the revocation path that matters.
    await request(app.server)
      .post('/auth/refresh')
      .send({ refreshToken: login.body.refreshToken })
      .expect(401);
  });

  it('rejects a duplicate email', async () => {
    await invite('dupe@example.com').expect(201);
    await invite('dupe@example.com').expect(409);
  });
});
```

- [ ] **Step 2: Run to verify it fails**

Run: `pnpm test:integration`
Expected: FAIL — `/admin/operators` returns 404

- [ ] **Step 3: Implement the operators repository**

`src/modules/operators/operators.repository.ts`:
```ts
import { Inject, Injectable } from '@nestjs/common';
import { eq } from 'drizzle-orm';
import { uuidv7 } from 'uuidv7';
import { DRIZZLE, type Db } from '../../infra/db/drizzle.module';
import { operators, setupTokens, users, type Operator } from '../../infra/db/schema';
import { hashToken } from '../../common/crypto/opaque-token';

export interface InviteInput {
  email: string;
  displayName: string;
  preferredLocale: string;
  bio?: string;
  gearTags?: string[];
}

@Injectable()
export class OperatorsRepository {
  constructor(@Inject(DRIZZLE) private readonly db: Db) {}

  /** User, profile, and invite token are created together or not at all. */
  async createInvited(
    input: InviteInput,
    setupTokenPlain: string,
    expiresAt: Date,
    adminUserId: string,
  ): Promise<{ operatorId: string; userId: string }> {
    return this.db.transaction(async (tx) => {
      const userId = uuidv7();
      const operatorId = uuidv7();

      await tx.insert(users).values({
        id: userId,
        role: 'operator',
        email: input.email,
        displayName: input.displayName,
        preferredLocale: input.preferredLocale,
        status: 'operator_pending_setup',
      });

      await tx.insert(operators).values({
        id: operatorId,
        userId,
        displayName: input.displayName,
        bio: input.bio ?? null,
        gearTags: input.gearTags ?? [],
      });

      await tx.insert(setupTokens).values({
        id: uuidv7(),
        userId,
        tokenHash: hashToken(setupTokenPlain),
        createdBy: adminUserId,
        expiresAt,
      });

      return { operatorId, userId };
    });
  }

  async findById(id: string): Promise<Operator | undefined> {
    const [row] = await this.db.select().from(operators).where(eq(operators.id, id)).limit(1);
    return row;
  }

  async approve(id: string, adminUserId: string): Promise<void> {
    await this.db
      .update(operators)
      .set({
        approvalStatus: 'approved',
        approvedAt: new Date(),
        approvedBy: adminUserId,
        updatedAt: new Date(),
      })
      .where(eq(operators.id, id));
  }

  async suspend(id: string): Promise<void> {
    await this.db
      .update(operators)
      .set({ approvalStatus: 'suspended', updatedAt: new Date() })
      .where(eq(operators.id, id));
  }

  async list(): Promise<Operator[]> {
    return this.db.select().from(operators);
  }
}
```

- [ ] **Step 4: Implement the service, DTO, and admin controller**

`src/modules/operators/dto/create-operator.dto.ts`:
```ts
import { createZodDto } from 'nestjs-zod';
import { z } from 'zod';

const supported = (process.env.SUPPORTED_LOCALES ?? 'en,he').split(',').map((s) => s.trim());

export const createOperatorSchema = z.object({
  email: z.string().email().max(320),
  displayName: z.string().min(1).max(120),
  preferredLocale: z.enum(supported as [string, ...string[]]).optional(),
  bio: z.string().max(2000).optional(),
  gearTags: z.array(z.string().max(60)).max(20).optional(),
});

export class CreateOperatorDto extends createZodDto(createOperatorSchema) {}
```

`src/modules/operators/operators.service.ts`:
```ts
import { Injectable } from '@nestjs/common';
import { ConfigService } from '@nestjs/config';
import ms from 'ms';
import { OperatorsRepository, type InviteInput } from './operators.repository';
import { UsersRepository, normalizeEmail } from '../users/users.repository';
import { SessionRepository } from '../auth/session.repository';
import { generateOpaqueToken } from '../../common/crypto/opaque-token';
import { ConflictError, NotFoundError } from '../../common/errors/domain-error';
import { ErrorCodes } from '../../common/errors/error-codes';
import type { Env } from '../../infra/config/env.schema';

@Injectable()
export class OperatorsService {
  constructor(
    private readonly repo: OperatorsRepository,
    private readonly usersRepo: UsersRepository,
    private readonly sessions: SessionRepository,
    private readonly config: ConfigService<Env, true>,
  ) {}

  async invite(input: Omit<InviteInput, 'preferredLocale'> & { preferredLocale?: string }, adminUserId: string) {
    const email = normalizeEmail(input.email);
    if (await this.usersRepo.findByEmail(email)) {
      throw new ConflictError(ErrorCodes.VALIDATION_FAILED, 'That email already exists.', {
        field: 'email',
      });
    }

    const setupToken = generateOpaqueToken();
    const ttl = ms(this.config.get('SETUP_TOKEN_TTL', { infer: true }) as ms.StringValue);
    const { operatorId } = await this.repo.createInvited(
      {
        ...input,
        email,
        preferredLocale:
          input.preferredLocale ?? this.config.get('DEFAULT_LOCALE', { infer: true }),
      },
      setupToken,
      new Date(Date.now() + ttl),
      adminUserId,
    );

    // The email itself is sent in sub-project #3's notification work; the token
    // is returned here so an admin can deliver the link out of band meanwhile.
    return { operatorId, setupToken };
  }

  async approve(operatorId: string, adminUserId: string): Promise<void> {
    const operator = await this.repo.findById(operatorId);
    if (!operator) {
      throw new NotFoundError(ErrorCodes.OPERATOR_NOT_FOUND, 'No such operator.');
    }
    await this.repo.approve(operatorId, adminUserId);
  }

  /**
   * Suspension must revoke refresh tokens immediately. The access token stays
   * valid until it expires (≤15 min), which spec §4.2 accepts explicitly.
   */
  async suspend(operatorId: string): Promise<void> {
    const operator = await this.repo.findById(operatorId);
    if (!operator) {
      throw new NotFoundError(ErrorCodes.OPERATOR_NOT_FOUND, 'No such operator.');
    }
    await this.repo.suspend(operatorId);
    await this.sessions.revokeAllForUser(operator.userId);
  }

  list() {
    return this.repo.list();
  }
}
```

`src/modules/operators/admin-operators.controller.ts`:
```ts
import { Body, Controller, Get, HttpCode, Param, ParseUUIDPipe, Post } from '@nestjs/common';
import { Roles } from '../../common/auth/roles.decorator';
import { CurrentUser } from '../../common/auth/current-user.decorator';
import type { AuthenticatedUser } from '../auth/auth.types';
import { OperatorsService } from './operators.service';
import { CreateOperatorDto } from './dto/create-operator.dto';

@Roles('admin')
@Controller('admin/operators')
export class AdminOperatorsController {
  constructor(private readonly operators: OperatorsService) {}

  @Post()
  create(@Body() dto: CreateOperatorDto, @CurrentUser() admin: AuthenticatedUser) {
    return this.operators.invite(dto, admin.userId);
  }

  @Get()
  list() {
    return this.operators.list();
  }

  @Post(':id/approve')
  @HttpCode(204)
  async approve(
    @Param('id', ParseUUIDPipe) id: string,
    @CurrentUser() admin: AuthenticatedUser,
  ) {
    await this.operators.approve(id, admin.userId);
  }

  @Post(':id/suspend')
  @HttpCode(204)
  async suspend(@Param('id', ParseUUIDPipe) id: string) {
    await this.operators.suspend(id);
  }
}
```

- [ ] **Step 5: Implement account setup in AuthService**

`src/modules/auth/dto/setup.dto.ts`:
```ts
import { createZodDto } from 'nestjs-zod';
import { z } from 'zod';

export const setupSchema = z.object({
  password: z.string().min(12).max(1024),
});

export class SetupDto extends createZodDto(setupSchema) {}
```

Append to `AuthService` (injecting `DRIZZLE` as `db`):
```ts
  /**
   * Consumes a one-time invite token: sets the password, activates the account,
   * and stamps the token used — all in one transaction so a crash between steps
   * cannot leave a spent token with no password set.
   */
  async completeSetup(tokenPlain: string, password: string): Promise<void> {
    const [row] = await this.db
      .select()
      .from(setupTokens)
      .where(eq(setupTokens.tokenHash, hashToken(tokenPlain)))
      .limit(1);

    if (!row || row.usedAt !== null || row.expiresAt.getTime() <= Date.now()) {
      throw new UnauthorizedError(
        ErrorCodes.SETUP_TOKEN_INVALID,
        'This setup link is invalid or has already been used.',
      );
    }

    const passwordHash = await this.passwords.hash(password);
    await this.db.transaction(async (tx) => {
      await tx
        .update(users)
        .set({ passwordHash, status: 'active', updatedAt: new Date() })
        .where(eq(users.id, row.userId));
      await tx.update(setupTokens).set({ usedAt: new Date() }).where(eq(setupTokens.id, row.id));
    });
  }
```

Append to `AuthController`:
```ts
  @Public()
  @Post('setup/:token')
  @HttpCode(204)
  async setup(@Param('token') token: string, @Body() dto: SetupDto) {
    await this.auth.completeSetup(token, dto.password);
  }
```

`src/modules/operators/operators.module.ts`:
```ts
import { Module } from '@nestjs/common';
import { AuthModule } from '../auth/auth.module';
import { UsersModule } from '../users/users.module';
import { AdminOperatorsController } from './admin-operators.controller';
import { OperatorsRepository } from './operators.repository';
import { OperatorsService } from './operators.service';

@Module({
  imports: [AuthModule, UsersModule],
  controllers: [AdminOperatorsController],
  providers: [OperatorsRepository, OperatorsService],
  exports: [OperatorsRepository, OperatorsService],
})
export class OperatorsModule {}
```

Add `OperatorsModule` to `src/app.module.ts`.

- [ ] **Step 6: Run to verify it passes**

Run: `pnpm test:integration`
Expected: PASS — 10 tests

- [ ] **Step 7: Commit**

```bash
git add .
git commit -m "feat: admin operator provisioning, invite setup flow, approve and suspend"
```

---

## Task 14: Operator Self-Service Profile

**Files:**
- Create: `src/modules/operators/operators.controller.ts`, `src/modules/operators/dto/update-profile.dto.ts`
- Modify: `src/modules/operators/operators.repository.ts`, `src/modules/operators/operators.service.ts`, `src/modules/operators/operators.module.ts`
- Test: `test/e2e/operator-profile.spec.ts`

**Interfaces:**
- Consumes: `@CurrentUser`, `@Roles`, `OperatorsRepository` (13).
- Produces: `OperatorsService.getOwnProfile(operatorId)`, `updateOwnProfile(operatorId, patch)`; `GET /operators/me`, `PATCH /operators/me`.

- [ ] **Step 1: Write the failing profile test**

Create `test/e2e/operator-profile.spec.ts`:
```ts
import { describe, expect, it, beforeAll, afterAll } from 'vitest';
import request from 'supertest';
import { uuidv7 } from 'uuidv7';
import { getTestDb } from '../integration/db.helper';
import { users, operators } from '../../src/infra/db/schema';
import { createTestApp, type TestApp } from './app.helper';
import { TokenService } from '../../src/modules/auth/token.service';

let app: TestApp;

beforeAll(async () => {
  app = await createTestApp();
});
afterAll(async () => {
  await app.close();
});

async function makeOperator(email: string) {
  const userId = uuidv7();
  const operatorId = uuidv7();
  await getTestDb()
    .insert(users)
    .values({ id: userId, role: 'operator', email, displayName: 'Pilot' });
  await getTestDb()
    .insert(operators)
    .values({ id: operatorId, userId, displayName: 'Pilot', approvalStatus: 'approved' });

  const tokens = app.app.get(TokenService);
  return {
    operatorId,
    auth: `Bearer ${tokens.issueAccessToken({ sub: userId, role: 'operator', operatorId, jti: uuidv7() })}`,
  };
}

describe('/operators/me', () => {
  it('returns the calling operator only', async () => {
    const me = await makeOperator('me1@example.com');
    const other = await makeOperator('me2@example.com');

    const res = await request(app.server)
      .get('/operators/me')
      .set('Authorization', me.auth)
      .expect(200);

    expect(res.body.id).toBe(me.operatorId);
    expect(res.body.id).not.toBe(other.operatorId);
  });

  it('never exposes a password hash', async () => {
    const me = await makeOperator('me3@example.com');
    const res = await request(app.server)
      .get('/operators/me')
      .set('Authorization', me.auth)
      .expect(200);
    expect(JSON.stringify(res.body)).not.toContain('passwordHash');
  });

  it('updates only the editable fields', async () => {
    const me = await makeOperator('me4@example.com');
    const res = await request(app.server)
      .patch('/operators/me')
      .set('Authorization', me.auth)
      .send({ displayName: 'Ace', bio: 'Ten years flying.', gearTags: ['mavic-3', 'fpv'] })
      .expect(200);

    expect(res.body).toMatchObject({
      displayName: 'Ace',
      bio: 'Ten years flying.',
      gearTags: ['mavic-3', 'fpv'],
    });
  });

  it('ignores an attempt to self-approve', async () => {
    const me = await makeOperator('me5@example.com');
    await getTestDb().update(operators).set({ approvalStatus: 'pending' });

    const res = await request(app.server)
      .patch('/operators/me')
      .set('Authorization', me.auth)
      .send({ displayName: 'Sneaky', approvalStatus: 'approved' })
      .expect(200);

    expect(res.body.approvalStatus).toBe('pending');
  });

  it('rejects a customer token with 403', async () => {
    const tokens = app.app.get(TokenService);
    const customer = `Bearer ${tokens.issueAccessToken({ sub: uuidv7(), role: 'customer', jti: uuidv7() })}`;
    await request(app.server).get('/operators/me').set('Authorization', customer).expect(403);
  });

  it('rejects an anonymous request with 401', async () => {
    await request(app.server).get('/operators/me').expect(401);
  });
});
```

- [ ] **Step 2: Run to verify it fails, then implement**

Run: `pnpm test:integration` → FAIL (404)

`src/modules/operators/dto/update-profile.dto.ts`:
```ts
import { createZodDto } from 'nestjs-zod';
import { z } from 'zod';

/**
 * Deliberately narrow. `approvalStatus` and `presence` are not editable here —
 * approval is an admin act, and presence is derived from check-in and booking
 * transitions. `.strip()` silently drops anything else rather than 422-ing, so
 * a client sending a whole profile object back does not fail.
 */
export const updateProfileSchema = z
  .object({
    displayName: z.string().min(1).max(120).optional(),
    bio: z.string().max(2000).nullable().optional(),
    gearTags: z.array(z.string().max(60)).max(20).optional(),
  })
  .strip();

export class UpdateProfileDto extends createZodDto(updateProfileSchema) {}
```

Append to `OperatorsRepository`:
```ts
  async updateProfile(
    id: string,
    patch: { displayName?: string; bio?: string | null; gearTags?: string[] },
  ): Promise<Operator> {
    const [row] = await this.db
      .update(operators)
      .set({ ...patch, updatedAt: new Date() })
      .where(eq(operators.id, id))
      .returning();
    return row!;
  }
```

Append to `OperatorsService`:
```ts
  async getOwnProfile(operatorId: string) {
    const operator = await this.repo.findById(operatorId);
    if (!operator) {
      throw new NotFoundError(ErrorCodes.OPERATOR_NOT_FOUND, 'No such operator.');
    }
    return operator;
  }

  async updateOwnProfile(operatorId: string, patch: Parameters<OperatorsRepository['updateProfile']>[1]) {
    await this.getOwnProfile(operatorId);
    return this.repo.updateProfile(operatorId, patch);
  }
```

`src/modules/operators/operators.controller.ts`:
```ts
import { Body, Controller, ForbiddenException, Get, Patch } from '@nestjs/common';
import { Roles } from '../../common/auth/roles.decorator';
import { CurrentUser } from '../../common/auth/current-user.decorator';
import type { AuthenticatedUser } from '../auth/auth.types';
import { OperatorsService } from './operators.service';
import { UpdateProfileDto } from './dto/update-profile.dto';

@Roles('operator')
@Controller('operators/me')
export class OperatorsController {
  constructor(private readonly operators: OperatorsService) {}

  @Get()
  get(@CurrentUser() user: AuthenticatedUser) {
    return this.operators.getOwnProfile(requireOperatorId(user));
  }

  @Patch()
  update(@CurrentUser() user: AuthenticatedUser, @Body() dto: UpdateProfileDto) {
    return this.operators.updateOwnProfile(requireOperatorId(user), dto);
  }
}

/**
 * The identity comes from the token, never a path parameter, so there is no
 * object-level authorization surface to get wrong on these routes.
 */
export function requireOperatorId(user: AuthenticatedUser): string {
  if (!user.operatorId) throw new ForbiddenException('Token carries no operator identity.');
  return user.operatorId;
}
```

Register `OperatorsController` in `OperatorsModule` controllers.

- [ ] **Step 3: Run to verify it passes**

Run: `pnpm test:integration`
Expected: PASS — 6 tests

- [ ] **Step 4: Commit**

```bash
git add .
git commit -m "feat: operator self-service profile read and update"
```

---

## Task 15: Authorization Matrix, OpenAPI, Admin Seed & CI

**Files:**
- Create: `test/e2e/authz-matrix.spec.ts`
- Create: `src/cli/seed-admin.ts`
- Create: `.github/workflows/ci.yml`
- Modify: `src/main.ts`, `package.json`
- Test: the matrix itself is the test

**Interfaces:**
- Consumes: every route from Tasks 8–14.
- Produces: `PROTECTED_ROUTES` table exported from `test/e2e/authz-matrix.spec.ts` — Phase 2 extends this same table; `pnpm seed:admin` CLI.

- [ ] **Step 1: Write the authorization matrix**

Create `test/e2e/authz-matrix.spec.ts`:
```ts
import { describe, expect, it, beforeAll, afterAll } from 'vitest';
import request from 'supertest';
import { uuidv7 } from 'uuidv7';
import { createTestApp, type TestApp } from './app.helper';
import { TokenService } from '../../src/modules/auth/token.service';
import type { Role } from '../../src/modules/auth/auth.types';

/**
 * The standing automated answer to the IDOR warning in the product design.
 * Every protected route gets a row. Adding an endpoint without adding a row is
 * a visible gap in review — that visibility is the point.
 */
export const PROTECTED_ROUTES: Array<{
  method: 'get' | 'post' | 'patch';
  path: string;
  allow: Role[];
  body?: Record<string, unknown>;
}> = [
  { method: 'post', path: '/auth/logout-all', allow: ['customer', 'operator', 'admin'] },

  { method: 'get', path: '/operators/me', allow: ['operator'] },
  { method: 'patch', path: '/operators/me', allow: ['operator'], body: { displayName: 'X' } },

  { method: 'post', path: '/admin/operators', allow: ['admin'], body: { email: 'a@b.co', displayName: 'A' } },
  { method: 'get', path: '/admin/operators', allow: ['admin'] },
  { method: 'post', path: `/admin/operators/${uuidv7()}/approve`, allow: ['admin'] },
  { method: 'post', path: `/admin/operators/${uuidv7()}/suspend`, allow: ['admin'] },
];

const ALL_ROLES: Role[] = ['customer', 'operator', 'admin'];

let app: TestApp;
let tokens: TokenService;

beforeAll(async () => {
  app = await createTestApp();
  tokens = app.app.get(TokenService);
});
afterAll(async () => {
  await app.close();
});

const tokenFor = (role: Role) =>
  tokens.issueAccessToken({
    sub: uuidv7(),
    role,
    operatorId: role === 'operator' ? uuidv7() : undefined,
    jti: uuidv7(),
  });

describe('authorization matrix', () => {
  it.each(PROTECTED_ROUTES)('$method $path rejects anonymous callers with 401', async (route) => {
    const res = await request(app.server)[route.method](route.path).send(route.body ?? {});
    expect(res.status).toBe(401);
  });

  it.each(
    PROTECTED_ROUTES.flatMap((route) =>
      ALL_ROLES.filter((r) => !route.allow.includes(r)).map((role) => ({ ...route, role })),
    ),
  )('$method $path rejects role $role with 403', async (route) => {
    const res = await request(app.server)
      [route.method](route.path)
      .set('Authorization', `Bearer ${tokenFor(route.role)}`)
      .send(route.body ?? {});

    // 403 specifically: authenticated but not permitted. A 401 here would mean
    // the role check never ran, and a 404 would mean the route silently moved.
    expect(res.status).toBe(403);
  });
});
```

- [ ] **Step 2: Run the matrix and fix any route it exposes**

Run: `pnpm test:integration`
Expected: PASS. Any 401 where 403 was expected means `@Roles` is missing from that route; any 404 means the path in the table is stale. Fix the route, not the table.

- [ ] **Step 3: Write the admin seed CLI**

Create `src/cli/seed-admin.ts`:
```ts
import 'reflect-metadata';
import { Pool } from 'pg';
import { drizzle } from 'drizzle-orm/node-postgres';
import { uuidv7 } from 'uuidv7';
import { users } from '../infra/db/schema';
import { PasswordService } from '../common/crypto/password.service';
import * as schema from '../infra/db/schema';

/**
 * Admins are provisioned out of band only — never through an HTTP route.
 * Usage: pnpm seed:admin admin@example.com 'a-long-password' 'Admin Name'
 */
async function main() {
  const [email, password, displayName] = process.argv.slice(2);
  if (!email || !password || !displayName) {
    throw new Error("Usage: pnpm seed:admin <email> <password> '<display name>'");
  }
  if (password.length < 12) {
    throw new Error('Password must be at least 12 characters.');
  }

  const pool = new Pool({ connectionString: process.env.DATABASE_URL });
  const db = drizzle(pool, { schema });
  try {
    await db.insert(users).values({
      id: uuidv7(),
      role: 'admin',
      email: email.trim().toLowerCase(),
      displayName,
      passwordHash: await new PasswordService().hash(password),
      status: 'active',
    });
    console.log(`Created admin ${email}`);
  } finally {
    await pool.end();
  }
}

main().catch((err: unknown) => {
  console.error(err instanceof Error ? err.message : err);
  process.exit(1);
});
```

Add to `package.json` scripts:
```json
{
  "seed:admin": "node -r ts-node/register src/cli/seed-admin.ts",
  "migrate": "drizzle-kit migrate"
}
```

Install the runner: `pnpm add -D ts-node`

- [ ] **Step 4: Enable OpenAPI generation**

In `src/main.ts`, before `app.listen`:
```ts
import { DocumentBuilder, SwaggerModule } from '@nestjs/swagger';
import { patchNestJsSwagger } from 'nestjs-zod';

patchNestJsSwagger(); // teaches Swagger to read zod DTOs

const openApi = new DocumentBuilder()
  .setTitle('ACS Backend')
  .setVersion('1.0')
  .addBearerAuth()
  .build();
SwaggerModule.setup('docs', app, SwaggerModule.createDocument(app, openApi));
```

Install: `pnpm add @nestjs/swagger`

- [ ] **Step 5: Write the CI workflow**

Create `.github/workflows/ci.yml`:
```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgis/postgis:16-3.4
        env:
          POSTGRES_USER: acs
          POSTGRES_PASSWORD: acs
          POSTGRES_DB: acs
        ports: ['5432:5432']
        options: >-
          --health-cmd "pg_isready -U acs"
          --health-interval 5s
          --health-retries 10
      redis:
        image: redis:7
        ports: ['6379:6379']
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-retries 10

    env:
      DATABASE_URL: postgres://acs:acs@localhost:5432/acs
      TEST_DATABASE_URL: postgres://acs:acs@localhost:5432/acs_test
      REDIS_URL: redis://localhost:6379
      TEST_REDIS_URL: redis://localhost:6379/1
      JWT_SECRET: ci-secret-that-is-long-enough-for-the-schema

    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with: { version: 9 }
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - run: pnpm install --frozen-lockfile
      - run: pnpm typecheck
      - run: pnpm lint
      - run: pnpm test:unit
      - run: pnpm test:integration
      - run: pnpm build
      - run: pnpm audit --audit-level high
```

- [ ] **Step 6: Run the full suite locally**

Run: `pnpm typecheck && pnpm test:unit && pnpm test:integration && pnpm build`
Expected: all green

- [ ] **Step 7: Commit**

```bash
git add .
git commit -m "feat: authorization matrix, admin seed CLI, OpenAPI docs, CI workflow"
```

---

## Phase 1 Completion Checklist

Before starting Phase 2, confirm:

- [ ] `pnpm typecheck`, `pnpm test:unit`, `pnpm test:integration`, and `pnpm build` all pass.
- [ ] `docker compose up -d && pnpm migrate && pnpm seed:admin ...` produces a usable admin.
- [ ] The authorization matrix in `test/e2e/authz-matrix.spec.ts` has a row for every route in the endpoint surface implemented so far.
- [ ] `.env` is not tracked by git; `.env.example` is.
- [ ] No route lacks either `@Public()` or an authentication path — spot-check by grepping controllers for `@Get`/`@Post`/`@Patch` and confirming each is accounted for.
